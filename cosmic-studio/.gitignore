# 参考资料与本地临时（不入库）
_ref/
*.log
export_p2.xlsx
tpl_test.xlsx

# Python
__pycache__/
*.pyc
.venv/

# 前端依赖（构建产物 dist/ 与 app/static/ 入库：mykng 无 Node，
# 流水线直接用入库产物 compose build，见 DEPLOY.md）
frontend/node_modules/

# 运行数据
data/
