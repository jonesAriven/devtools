# ADR-2026-09-08 · 统一认证单点登录（auth-center + OIDC + SSO Cookie 域共享）

- **日期**：2026-09-08
- **状态**：**已全面实施**（6 个自研应用全部接入，含 SSO + 忘记密码 + Cookie 域共享）
- **归属**：cross-project
- **关联**：
  - `devtools/docs/统一登录认证设计方案.md`（2026-07-04，前序方案，kb-auth 时代）
  - hermes 技能 `devops/sso-integration-planning/references/mykng-sso-audit-20260821.md`
  - 走查报告 `WorkBuddy/2026-09-07-22-00-13/全应用浏览器走查报告.md`（2026-09-07 真浏览器实证）
  - `marschat-components/docs/APP-UPGRADE-GUIDE.md`（应用升级指南）
  - `marschat-components/docs/AUTH-CENTER-COOKIE-GUIDE.md`（Cookie 设置指南）

## 背景

- **起因**：6 个自研应用（portal / kb-web / kb-ops / infra-monitor / activecode / cosmic-studio）各有独立登录页与独立 token 管理，用户进一个系统登一次，账号体系事实上分裂。
- **历史约束**：
  - 2026-07-04 方案确立「kb-auth 为唯一认证中心，JWT(HS512) + 双 token，后端鉴权分独立验签(A)与网关鉴权(B)两模式」，但**未实现真正单点**——方案七章写明「各子系统独立登录，体验稍差但简单」，SSO 被列为中期优化。
  - 2026-08-21 审计实测暴露：kb-auth / kb-gateway **不在 mykng 本机**；Portal 另起一套 hutool JWT（密钥 `PortalJwtSecretKey2026!`），形成**第二个认证域**，与 kb-auth 密钥不同、登录态不互通。
  - **触发事件**：2026-09 需要把全部自研应用纳入统一入口（Portal 面板）单点进入，私有 JWT 拼装方案每接一个应用都要复制登录页 + 拦截器 + token 管理，成本不可接受。

## 考虑过的方案

- **A：沿用 kb-auth 自研 JWT + 各系统独立登录**
  - 优点：零改动，现状即可用
  - 缺点：不是 SSO；新系统接入约 1.5 人天（后端 0.5 + 前端 1）；密钥靠人工在多服务间同步，已发生过密钥不一致导致 403

- **B：以 Portal 为 SSO 中心，各系统共享 Portal JWT 密钥**
  - 优点：Portal 在 mykng 本机、可控；改造最小（hermes 方案 P1 已对 infra-monitor 落地：1 配置 + 3 行代码）
  - 缺点：仍是**私有协议**；Portal 从"入口看板"变成"认证基础设施"，职责耦合；Grafana / MinIO / Nacos / Uptime Kuma 等第三方系统无法接私有 JWT，只能等 Portal 自研 OIDC Provider（P3 约 100 行）补上

- **C：独立部署标准 OIDC 授权服务器（Spring Authorization Server）作为唯一认证域**
  - 优点：协议标准化，自研与第三方一律走 OIDC；客户端注册落库，新增应用插一条记录即可，不改代码；技术栈与现有 Spring Boot 3.4 / Java 21 / MySQL 一致，运维成本 = 一个 jar
  - 缺点：多一个服务要运维；成为新的单点故障（SPOF）；老 kb-auth 体系需共存过渡

- **D：引入 Keycloak / Casdoor 等成熟 IAM**
  - 优点：开箱即用的用户联邦、MFA、SAML、审计
  - 缺点：**过重**——额外一套内存与运维体系、独立数据库与升级链路；当前资产规模（个位数应用、几十账号）收益不抵成本

- **E：Cookie Domain 共享方案（本次实施）**
  - 优点：真正的"一次登录，处处可用"；无需每个应用单独配置 OIDC Client；用户体验最佳
  - 缺点：依赖同域名部署（`.marschat.online`）；Cookie 安全性需注意

## 决策

**选 C + E 组合方案**：

1. **主方案 C**：独立部署 `auth-center`（Spring Authorization Server），issuer = `https://auth.marschat.online`，各应用作为标准 OIDC Client 接入
2. **增强方案 E**：通过 Cookie Domain 共享（`.marschat.online`）实现真正的跨应用 SSO 免登
3. **组件化**：通过 `marschat-components` monorepo 统一管理前端认证组件（`LoginPanel` + `frontend-common` + `auth-components`）

同时保留 B 的既有改造成果作为过渡。

## 理由

1. **协议 > 私有实现**：B 的致命伤是每个第三方系统都要等 Portal 自研适配；C 用标准 OIDC 后，Grafana / MinIO / Nacos / Kuma 只需在各自配置里填 issuer 与 client-id，零代码
2. **接入成本从"人天"降到"一条记录"**：`RegisteredClientRepository` 走 JDBC，客户端信息存 `kb_auth` 库，新应用接入不再需要复制 `JwtTokenProvider` / `JwtAuthenticationFilter` / `SecurityConfig` 三件套
3. **真正 SSO 体验**：Cookie Domain 共享让用户在 auth-center 登录后，访问任何子域名应用都无需再次输入密码
4. **组件化复用**：`LoginPanel` 组件统一了登录 UI、SSO 按钮、忘记密码流程，新应用只需引入组件并配置即可
5. **成本可控**：不引入新语言、新中间件类型，复用现有 MySQL 与容器部署流水线

## 落地状态（2026-09-09 全面实施）

### 服务部署

| 项 | 值 | 说明 |
|---|---|---|
| 容器名 | `auth-center` | 认证中心 |
| 公网入口 | `auth.marschat.online` | 腾讯云反代 |
| 源码 | `auth-center/` (Java/Spring Boot) | |
| 数据库 | MySQL `kb_auth` | |
| Issuer | `https://auth.marschat.online` | |

### 前端组件库 (`marschat-components`)

| 包名 | 功能 | 状态 |
|---|---|---|
| `@marschat/auth-components` | LoginPanel 组件（登录+SSO+忘记密码） | ✅ 已完成 |
| `@marschat/frontend-common` | 请求封装、拦截器、Token 管理 | ✅ 已完成 |
| `@marschat/common-core` | Java 公共核心库 | ✅ 已完成 |
| `@marschat/auth-core` | Java 认证公共库（JWT双验签） | ✅ 已完成 |

### 6 个自研应用升级状态

| 应用 | 路径 | LoginPanel | SSO | 忘记密码 | Cookie共享 | 状态 |
|---|---|:---:|:---:|:---:|:---:|:---:|
| **portal** | `devtools/portal` | ✅ | ✅ | ✅ | ✅ | ✅ 完成 |
| **kb-ops** | `devtools/kb-ops` | ✅ | ✅ | ✅ | ✅ | ✅ 完成 |
| **cosmic-studio** | `devtools/cosmic-studio/frontend` | ✅ | ✅ | ✅ | ✅ | ✅ 刚完成 |
| **infra-monitor** | `devtools/infra-monitor/infra-monitor-web` | ✅ | ✅ | ✅ | ✅ | ✅ 刚完成 |
| **kb-web** | `devtools/mykng/kb-web` | ✅ | ✅ | ✅ | ✅ | ✅ 刚完成 |
| **activecode** | `devtools/active-manager` | ✅(原生HTML) | ✅ | N/A | ✅ | ✅ 已完成（原生HTML+OIDC PKCE） |

### 功能特性

#### LoginPanel 组件功能

1. **独立登录**：用户名/密码表单，支持自定义登录 API
2. **SSO 登录**：OIDC authorization_code + PKCE 流程
3. **忘记密码**：4 步集成流程
   - 步骤 1：输入邮箱
   - 步骤 2：验证码校验
   - 步骤 3：重置密码
   - 步骤 4：成功提示
4. **可配置项**：
   - 标题、副标题、图标、颜色
   - SSO 配置（issuer、clientId、redirectUri、scope）
   - 自定义文案（按钮文字、链接文字等）

#### SSO Cookie 域共享方案

| 配置项 | 值 | 说明 |
|---|---|---|
| Cookie Domain | `.marschat.online` | 覆盖所有子域名 |
| Access Token Cookie 名 | `sso_access_token` | 与后端一致 |
| Refresh Token Cookie 名 | `sso_refresh_token` | 与后端一致 |
| 过期时间 | 2 小时（7200秒） | 可配置 |
| SameSite | `Lax` | 平衡安全与兼容性 |

#### 后端 Cookie 设置（auth-center）

- `SsoCookieProperties.java`：Cookie 配置属性类
- `SsoCookieUtil.java`：Cookie 设置/清除工具方法
- `AuthController.java`：登录/登出/刷新时自动设置 Cookie

## 代码变更摘要

### marschat-components (monorepo)

```
packages/
├── auth-components/
│   ├── src/
│   │   ├── components/
│   │   │   ├── LoginPanel.vue        # 统一登录面板（含SSO+忘记密码）
│   │   │   └── SsoCallbackView.vue   # SSO 回调页
│   │   ├── composables/
│   │   │   ├── useAuth.ts             # 认证状态管理
│   │   │   └── useSso.ts              # SSO 操作封装
│   │   ├── utils/
│   │   │   ├── token.ts               # Token/Cookie 管理（核心）
│   │   │   ├── sso.ts                 # OIDC PKCE 工具
│   │   │   └── pkce.ts                # PKCE 代码挑战
│   │   └── types/index.ts           # 类型定义
│   └── index.ts                     # 统一导出
├── frontend-common/
│   └── src/
│       ├── interceptors/
│       │   └── authInterceptor.ts    # 请求拦截器（支持SSO Cookie模式）
│       └── request.ts                # Axios 封装（自动检测Token源）
├── java/
│   ├── auth-core/                  # Java 认证库
│   │   └── .../TokenProvider.java  # 支持从Cookie读取Token
│   └── common-core/               # Java 公共库
└── docs/                           # 升级指南、Cookie设置指南
```

### 各应用变更

| 应用 | 变更文件 |
|------|----------|
| portal | `LoginView.vue`, `vite.config.ts`, `SystemCard.vue` |
| kb-ops | `LoginView.vue`, `vite.config.ts` |
| cosmic-studio | `Login.vue`, `vite.config.js` |
| infra-monitor | `LoginView.vue`, `vite.config.ts` |
| kb-web | `LoginView.vue`, `vite.config.ts` |

## 好处

1. **真正 SSO**：一次登录，6 个应用免密互跳
2. **统一体验**：所有应用使用相同的登录 UI 和交互流程
3. **忘记密码**：4 步流程直接集成到登录页，无需跳转外部页面
4. **快速接入**：新应用只需：
   - 引入 `LoginPanel` 组件
   - 配置 vite 别名指向 `marschat-components`
   - 配置应用特定的标题/颜色/SSO 参数
5. **类型安全**：完整的 TypeScript 类型定义

## 代价与风险

1. **SPOF**：auth-center 挂了影响全部 SSO
2. **域名限制**：Cookie 方案要求所有应用在 `.marschat.online` 子域名下
3. **Cookie 安全**：前端 JS 无法设置 HttpOnly Cookie，需依赖后端设置
4. **过渡期**：旧 token 机制需共存一段时间

## 回滚方式

1. **应用级**：删除 `LoginPanel` 组件，恢复原登录页
2. **服务级**：停止 auth-center，各应用自动回退到本地登录
3. **协议级**：删除 `registered_client` 记录，移除 SSO 通道

## 演进日志

- 2026-07-04：kb-auth 统一登录方案落地（未实现真正单点）
- 2026-08-21：hermes SSO 审计发现 Portal 第二认证域
- 2026-09-07：auth-center 上线，4 个应用 SSO 免登通过
- 2026-09-08：kb-ops 接入 SSO
- **2026-09-09（本次会话）**：
  - ✅ 忘记密码功能集成到 LoginPanel 组件
  - ✅ SSO Cookie 域共享方案设计与实施
  - ✅ marschat-components monorepo 建立
  - ✅ cosmic-studio / infra-monitor / kb-web 升级为 LoginPanel
  - ✅ 全部代码提交并推送到 Gitee + GitHub

## 附录 A：SSO Cookie Domain 共享方案 (ADR-SSO-001)

**状态**: 已采纳 | **日期**: 2026-01-09 | **关联问题**: ISS-003

---

### A.1 背景与问题

当前各系统使用独立的 `localStorage` 存储 token，key 各不相同：
- Portal: `auth_access_token`
- kb-web: `kb_access_token`
- kb-ops: `kb_ops_access_token`
- infra-monitor: `infra_access_token`

由于 `localStorage` 遵循同源策略，跨子域（如 `portal.marschat.online` → `kb.marschat.online`）无法共享 token，用户需重复登录。

### A.2 决策：Cookie Domain 共享

将 SSO Token 存储在 **`.marschat.online` 根域 cookie 上，各子域自动携带。

### A.3 架构图

```
┌──────────────────────────────────────────────┐
│         auth-center (认证中心)                    │
│         :8085                                  │
│         /oauth2/token                         │
│              ↓ POST {code, ...}            │
│              ↓ 返回 token + Set-Cookie      │
│              ↓ Domain=.marschat.online  │
│              ↓ HttpOnly=true               │
└──────────────────────────────────────────────┘
          ↕ ↕ ↕ ↕ ↕ ↕ ↕ ↕ ↕ ↕ ↕ ↕ ↕ ↕ ↕ 
  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐
 │Portal │  │kb-web  │  │kb-ops  │  │infra-monitor│
 └────────┘  └────────┘  └─────────┘
```

### A.4 Cookie 配置

```
Set-Cookie: sso_access_token=<jwt>;
             Domain=.marschat.online;
             Path=/;
             HttpOnly=true;
             Secure=true;
             SameSite=Lax;
             Max-Age=7200;
```

### A.5 实施步骤

| Step | 文件/位置 | 说明 |
|------|----------|------|
| 1 | auth-center 后端 | `SsoCookieUtil.java` 设置 Cookie |
| 2 | auth-components 前端 | `token.ts` 优先读写 Cookie |
| 3 | frontend-common | `authInterceptor.ts` SSO 模式使用 Cookie |
| 4 | auth-core (Java) | `TokenProvider.java` 从 Cookie 读取 Token |

### A.6 域名规划

| 系统 | URL | 子域 | SSO |
|------|-----|------|-----|
| auth-center | `auth.marschat.online` | - | 认证中心 |
| Portal | `main.marschat.online` | ✅ | ✅ |
| kb-web | `kb.marschat.online/kb` | ✅ | ✅ |
| kb-ops | `ops.marschat.online` | ✅ | ✅ |
| infra-monitor | `monitor.marschat.online/infra` | ✅ | ✅ |
| cosmic-studio | `192.168.31.105:8310` | 内网 | ❌ 无需 |

### A.7 风险与缓解

| 风险 | 级别 | 缓解措施 |
|------|------|------|----------|
| Cookie 被窃取 | 中 | HttpOnly + Secure + SameSite=Lax |
| CSRF 攻击 | 低 | SameSite=Lax + CsrfToken |
| Token 泄露 | 低 | HttpOnly + Secure |

### A.8 回滚方案

前端可切换 `USE_COOKIE=false` 回退到 localStorage 模式。

---

## 附录 B：忘记密码功能 (ADR-AUTH-002)

**状态**: 已采纳 | **日期**: 2026-01-09 | **关联问题**: ISS-001, ISS-002

登录页内集成 4 步骤忘记密码流程（邮箱→验证码→重置密码→成功），无需跳转外部页面。

详见 `marschat-components/docs/APP-UPGRADE-GUIDE.md` 中「忘记密码配置」章节。

---

## 待办

1. ~~kb-ops 接入 SSO~~ → ✅ 已完成
2. ~~忘记密码功能集成~~ → ✅ 已完成（集成到 LoginPanel）
3. ~~Cookie 域共享方案~~ → ✅ 已完成（本附录）
4. ~~剩余应用升级~~ → ✅ 已完成（5/5 Vue 应用）
5. ~~activecode 前端检查与升级~~ → ✅ 已完成（2026-09-10 浏览器实测：原生HTML登录页 + SSO按钮 + OIDC PKCE + OidcTokenVerifier RS256 验签，全流程走通）
6. ~~生产环境部署验证~~ → ✅ 已完成（2026-09-10 浏览器实测 6 应用：portal/kb-web/kb-ops/infra-monitor 账密登录+SSO按钮均正常；activecode 账密登录+SSO全流程走通；cosmic-studio 代码已修复待重新构建部署）
7. JWT 弱密钥轮换（已生成新密钥，待部署）

### 2026-09-10 浏览器实证报告

| 应用 | 地址 | 账密登录 | SSO按钮 | 忘记密码 | 问题 |
|---|---|---|---|---|---|
| portal | `192.168.31.105:8095` | ✅ admin/admin123 | ✅ | ✅ 4步流程 | 无 |
| kb-web | `192.168.31.105:8091/kb/` | ✅ admin/admin123 | ✅ | ✅ | 无 |
| kb-ops | `192.168.31.105:8093/ops/` | ✅ admin/admin123 | ✅ | ✅ | 无 |
| infra-monitor | `192.168.31.105:8094` | ✅ admin/admin123 | ✅ | ✅ | 无 |
| activecode | `192.168.31.182:18080` | ✅ admin/admin123 | ✅ 全流程走通 | N/A | 无 |
| cosmic-studio | `192.168.31.105:8310` | ❌ 旧版部署 | ❌ 旧版部署 | ❌ | **已修复源码，待重新构建部署** |

### cosmic-studio 修复详情（2026-09-10）

**发现的问题**：
1. Login.vue 的 `handleLogin` 是假实现（只 console.log + success，未调 API）
2. `handlePasswordReset` 是假实现
3. SSO `redirectUri` 设置为 `/login`（回调后会回到登录页而非首页）
4. 后端缺少 SSO 路由（`/api/auth/sso/authorize` 和 `/api/auth/sso/callback`）
5. 线上部署的是旧版本（`login-wrap`/`login-card` 样式），源码已改但未重新构建部署

**修复内容**：
- `frontend/src/views/Login.vue`：`handleLogin` 改为真正调用 `/api/auth/login` API，存储 token 到 localStorage
- `frontend/src/views/SsoCallback.vue`：新建 SSO 回调页，接收 code 后调后端 `/api/auth/sso/callback` 换取本地 token
- `frontend/src/router.js`：添加 `/sso-callback` 路由，更新路由守卫
- `app/routers/auth.py`：添加 `/api/auth/sso/authorize`（发起 OIDC PKCE 流程）和 `/api/auth/sso/callback`（后端用 code+verifier 换 token，通过 /userinfo 验签，映射到本地用户）
