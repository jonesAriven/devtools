开发两个工具，一个是生成激活码的工具，一个是验证激活码的工具。
生成激活码的工具，每次调用激活码工具，都会生成一个新的激活码。
  输入：唯一的序列号=软件初始序列号+机器码（可以用mac地址作为机器码）后得到的加密串。唯一序列号是软件在不同终端登录的唯一标识
  输出：激活码=唯一的序列号+软件过期时间时间戳后得到的加密串
  其他要求：
  1. 并且根据输入的唯一的序列号，都需要根据唯一序列号记住他的激活记录。这个需要记录到数据库中。我会提供数据库的连接信息。
  2. 这个工具请使用Java开发。并且有个javaweb界面，用户可以在浏览器中调用。
  3. 这个工具需要提供一个restful接口，用户可以在软件中调用。
  4. 这个工具需要提供一个数据库连接池，用于连接数据库。
  5. 这个工具需要提供一个日志记录器，用于记录激活码工具的运行日志。
  6. 这个工具需要提供一个配置文件，用于配置数据库连接信息。 这个配置文件需要包含数据库的连接字符串。
  7. 激活码不可破解，只能通过激活码工具生成。

验证激活码的工具：
  输入：激活码=唯一的序列号+软件过期时间时间戳后得到的加密串
  输出：
        1. 激活成功或激活失败，激活成功则让软件正常运行，若激活失败，则软件会提示激活失败。
        2. 唯一的序列号=软件初始序列号+机器码（可以用mac地址作为机器码）。唯一序列号是软件在不同终端登录的唯一标识。
  其他要求：
  1. 验证激活码的工具需要在软件中嵌入，用户在软件中调用验证激活码的工具，即可验证激活码是否有效。
  2. 验证激活码的工具需要根据激活码中的唯一序列号，来验证激活码是否有效。
  3. 验证激活码的工具需要根据激活码中的过期时间时间戳，来验证激活码是否过期。
  4. 激活码工具可以嵌入到各种语言开发的软件中。请提供嵌入到各种语言开发的软件中的嵌入方法。优先考虑如何嵌入C#开发的软件中。
  5. 验证激活工具不可破解，只能通过验证激活码工具验证。
