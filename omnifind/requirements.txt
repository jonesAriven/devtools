# OmniFind 依赖(全离线自包含)
# --- Web 服务 ---
fastapi
uvicorn[standard]
pyyaml

# --- 中文分词(L2 全文层)---
jieba

# --- 文档抽取器(覆盖所有文档类型)---
pymupdf          # PDF
python-docx      # Word .docx
openpyxl         # Excel .xlsx
python-pptx      # PowerPoint .pptx

# --- L1 增量监听(阶段二)---
watchdog

# --- L3 语义层(阶段四,本地 CPU 推理,离线自包含)---
onnxruntime       # 本地推理(CPU, int8 量化模型)
tokenizers        # BGE 分词
lancedb           # 嵌入式向量库(无服务、内存映射)
numpy
