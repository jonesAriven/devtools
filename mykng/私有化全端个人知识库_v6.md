# Private-Knowledge-Base 微服务重构与实现计划

## 架构设计

### 服务拆分（轻量微服务，适配单机部署）

```
┌─────────────────────────────────────────────────────────┐
│                    Nginx (443)                            │
│              tools.marschat.online/kb/*                   │
└─────────────────────┬───────────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────────────────┐
│              kb-gateway (端口 8080)                       │
│  Spring Cloud Gateway + JWT Auth Filter + 静态资源       │
│  路由：/api/auth/* → kb-auth:8081                        │
│        /api/file/* → kb-file:8082                        │
│        /api/*      → kb-knowledge:8083                   │
│        /api/ops/*  → kb-ops:8084                         │
│        /s/*        → 静态资源                             │
└─────────────────────┬───────────────────────────────────┘
     │           │           │           │
     ▼           ▼           ▼           ▼
┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
│ kb-auth │ │ kb-file │ │kb-know- │ │ kb-ops  │
│ :8081   │ │ :8082   │ │ledge    │ │ :8084   │
│         │ │         │ │ :8083   │ │         │
│ 用户    │ │ 文件    │ │         │ │ 运维    │
│ JWT     │ │ 解析    │ │ 目录    │ │ 主机    │
│ Token   │ │ MinIO   │ │ 笔记    │ │ 端口    │
│         │ │         │ │ 网页    │ │ 账密    │
│         │ │         │ │ 搜索    │ │ 看板    │
│         │ │         │ │ 分享    │ │ 矛盾    │
└─────────┘ └─────────┘ └─────────┘ └─────────┘
     │           │           │           │
     └───────────┼───────────┼───────────┘
                 ▼           ▼
          ┌──────────┐ ┌──────────┐
          │ MySQL    │ │ MongoDB  │
          │ 8.0      │ │ 7.0      │
          └──────────┘ └──────────┘
          ┌──────────┐ ┌──────────┐
          │ Redis    │ │ MinIO    │
          │ 7        │ │          │
          └──────────┘ └──────────┘
          ┌──────────┐
          │ Meili-   │
          │ Search   │
          └──────────┘
```

### 数据库拆分（各服务独立 schema）

| 服务 | 数据库 | 表 |
|------|--------|-----|
| kb-auth | kb_auth | user, refresh_token, jwt_blacklist, ops_api_token |
| kb-file | kb_file | file, file_chunk + MongoDB file_content |
| kb-knowledge | kb_knowledge | folder, doc, web_page, tag, resource_tag, share, share_access_log, version, operation_log, bucket + MongoDB doc_content, web_content |
| kb-ops | kb_ops | ops_host, ops_port, ops_credential, ops_domain, ops_dependency, ops_change_log, ops_conflict, ops_snapshot |

### 服务间通信

- **同步**：OpenFeign（服务间 API 调用）
- **异步**：RabbitMQ（文件解析完成通知等，后续）
- **服务发现**：无（单机部署，直接 HTTP URL）

### 服务依赖方向（P0 新增）

**严格分层，禁止下层调用上层，禁止同层循环调用：**

```
┌─────────────────────────────────────────────────────────────┐
│                      kb-gateway (:8080)                      │
│              统一入口 + JWT 鉴权 + 静态资源                    │
└──────┬──────────┬──────────────┬────────────┬──────────────┘
       │          │              │            │
       ▼          ▼              ▼            ▼
  ┌─────────┐ ┌─────────┐ ┌───────────┐ ┌─────────┐
  │ kb-auth │ │ kb-file │ │kb-know-   │ │ kb-ops  │
  │ :8081   │ │ :8082   │ │ledge      │ │ :8084   │
  │         │ │         │ │ :8083     │ │         │
  └─────────┘ └────┬────┘ └─────┬─────┘ └────┬────┘
                   │            │            │
                   │  Feign调用  │            │
                   └───────────►│            │
                                │            │
                                │  Feign调用  │
                                └───────────►│
                                              │
                   ┌──────────────────────────┘
                   │  kb-ops 可调用 kb-knowledge（查目录树）
                   │  kb-ops 可调用 kb-auth（验证 Token）
                   ▼
              【下层服务不反向调用上层】
```

**依赖规则：**

| 调用方 → 被调用方 | 允许 | 说明 |
|-------------------|:----:|------|
| kb-gateway → 任意服务 | ✅ | 网关是入口，可调用所有下游 |
| kb-file → kb-knowledge | ✅ | 文件解析完成后通知建索引 |
| kb-ops → kb-knowledge | ✅ | 运维数据关联目录树 |
| kb-ops → kb-auth | ✅ | 验证 API Token |
| kb-knowledge → kb-auth | ✅ | 验证用户身份 |
| kb-knowledge → kb-file | ⚠️ | **写操作禁止**（通过事件解耦）；**读操作允许**（分享详情、回收站、搜索补全文件元数据，需同步调用）|
| kb-auth → 任意服务 | ❌ | **禁止**，auth 是最底层服务 |
| kb-file → kb-ops | ❌ | **禁止**，无业务关联 |
| 同层互相调用 | ❌ | **禁止循环依赖** |

**解耦规则：**
- 当下层需要通知上层时（如 kb-file 解析完成需要 kb-knowledge 建索引），**不直接 Feign 调用**，改为通过 Redis Pub/Sub 或后续 RabbitMQ 事件通知
- 事件格式统一：`{ "event": "file.parsed", "fileId": 123, "content": "...", "timestamp": "..." }`

### 技术栈

| 组件 | 选型 |
|------|------|
| 基础框架 | Spring Boot 3.2 + JDK 21 |
| 网关 | Spring Cloud Gateway 4.1 |
| 服务间调用 | OpenFeign |
| ORM | MyBatis-Plus 3.5.6 |
| 认证 | JWT (jjwt 0.12.5) |
| 搜索引擎 | MeiliSearch |
| 对象存储 | MinIO |
| 缓存 | Redis |
| 内容存储 | MongoDB |
| 密码加密 | AES-256-GCM |
| 前端 | Vue3 + ElementPlus + Vite（不变）|
| 链路追踪 | MDC + RequestId（P0 新增） |
| 日志框架 | Logback（P1 统一格式） |

---

## P0 优化：统一返回格式 + 全局异常处理

### 统一返回格式（kb-common 模块）

所有服务统一使用 `Result<T>` 包装返回值：

```java
// kb-common: com.kb.common.result.Result
public class Result<T> {
    private int code;        // 200=成功, 400=参数错误, 401=未认证, 403=无权限, 500=服务异常
    private String message;
    private T data;
    private String traceId;  // 链路追踪 ID（P0 新增）

    public static <T> Result<T> success(T data) { ... }
    public static <T> Result<T> error(int code, String message) { ... }
}
```

**前端统一处理：**
```js
// axios 响应拦截器
axios.interceptors.response.use(res => {
  if (res.data.code === 200) return res.data.data;
  if (res.data.code === 401) { /* 跳转登录 */ }
  ElMessage.error(res.data.message);
  return Promise.reject(res.data);
});
```

### 全局异常处理（kb-common 模块）

```java
// kb-common: com.kb.common.exception.GlobalExceptionHandler
@RestControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(BusinessException.class)
    public Result<?> handleBusiness(BusinessException e) {
        log.warn("业务异常: {}", e.getMessage());
        return Result.error(e.getCode(), e.getMessage());
    }

    @ExceptionHandler(NotLoginException.class)
    public Result<?> handleNotLogin(NotLoginException e) {
        return Result.error(401, "未登录或登录已过期");
    }

    @ExceptionHandler(NoPermissionException.class)
    public Result<?> handleNoPermission(NoPermissionException e) {
        return Result.error(403, "无操作权限");
    }

    @ExceptionHandler(MethodArgumentNotValidException.class)
    public Result<?> handleValidation(MethodArgumentNotValidException e) {
        String msg = e.getBindingResult().getFieldErrors().stream()
            .map(f -> f.getField() + ": " + f.getDefaultMessage())
            .collect(Collectors.joining("; "));
        return Result.error(400, "参数校验失败: " + msg);
    }

    @ExceptionHandler(Exception.class)
    public Result<?> handleUnknown(Exception e) {
        log.error("未知异常", e);
        return Result.error(500, "服务内部错误，请稍后重试");
    }
}
```

**自定义异常体系：**
```
RuntimeException
  └── BusinessException (业务异常基类)
        ├── NotLoginException      (401)
        ├── NoPermissionException  (403)
        ├── NotFoundException      (404, 资源不存在)
        ├── ConflictException      (409, 数据冲突)
        └── FileParseException     (422, 文件解析失败)
```

---

## P0 优化：链路追踪（traceId）

### 实现方案：MDC + 网关注入

**1. 网关生成 traceId 并注入请求头：**

```java
// kb-gateway: TraceIdFilter
@Component
public class TraceIdFilter implements GlobalFilter, Ordered {
    private static final String TRACE_ID = "X-Trace-Id";

    @Override
    public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {
        String traceId = exchange.getRequest().getHeaders().getFirst(TRACE_ID);
        if (StrUtil.isBlank(traceId)) {
            traceId = UUID.randomUUID().toString().replace("-", "");
        }
        String finalTraceId = traceId;
        return chain.filter(exchange.mutate()
            .request(exchange.getRequest().mutate()
                .header(TRACE_ID, finalTraceId)
                .build())
            .build())
            .then(Mono.fromRunnable(() ->
                exchange.getResponse().getHeaders().add(TRACE_ID, finalTraceId)));
    }

    @Override
    public int getOrder() { return Ordered.HIGHEST_PRECEDENCE; }
}
```

**2. 下游服务从请求头提取 traceId 放入 MDC：**

```java
// kb-common: TraceIdInterceptor
public class TraceIdInterceptor implements HandlerInterceptor {
    @Override
    public boolean preHandle(HttpServletRequest req, HttpServletResponse resp, Object handler) {
        String traceId = req.getHeader("X-Trace-Id");
        if (StrUtil.isBlank(traceId)) {
            traceId = UUID.randomUUID().toString().replace("-", "");
        }
        MDC.put("traceId", traceId);
        resp.setHeader("X-Trace-Id", traceId);
        return true;
    }

    @Override
    public void afterCompletion(HttpServletRequest req, HttpServletResponse resp, Object handler, Exception ex) {
        MDC.remove("traceId");
    }
}
```

**3. Feign 调用时自动传递 traceId：**

```java
// kb-common: FeignTraceIdInterceptor
@Bean
public RequestInterceptor feignTraceIdInterceptor() {
    return template -> {
        String traceId = MDC.get("traceId");
        if (StrUtil.isNotBlank(traceId)) {
            template.header("X-Trace-Id", traceId);
        }
    };
}
```

**4. Result 返回值携带 traceId（方便前端报错时上报）：**

```java
// 在 GlobalExceptionHandler 中注入
String traceId = MDC.get("traceId");
return Result.error(code, message).withTraceId(traceId);
```

**排查示例：**
```
# 拿到前端报错的 traceId，在日志中一键定位全链路
grep "traceId=a1b2c3d4" /data/logs/kb-*.log
```

---

## P1 优化：数据最终一致性方案

### 问题场景

| 场景 | 涉及服务 | 风险 |
|------|---------|------|
| 文件删除后索引未删 | kb-file + kb-knowledge | 搜索命中已删文件 |
| 文件解析成功但索引写入失败 | kb-file + kb-knowledge | 文件存在但搜不到 |
| 运维数据导入但快照未更新 | kb-ops 内部 | 快照数据不准 |
| 分享链接对应文件已删 | kb-knowledge 内部 | 分享打开 404 |

### 方案：事件通知 + 定时补偿（双重保障）

**第一层：实时事件通知（Redis Pub/Sub）**

```java
// 事件发布（kb-file 文件解析完成后）
@Service
public class FileParseService {
    @Autowired
    private RedisTemplate<String, String> redisTemplate;

    public void onParseComplete(Long fileId, String content) {
        KbEvent event = new KbEvent("file.parsed", fileId,
            Map.of("content", content), Instant.now());
        redisTemplate.convertAndSend("kb:events", JSON.toJSONString(event));
    }

    public void onFileDeleted(Long fileId) {
        KbEvent event = new KbEvent("file.deleted", fileId,
            Map.of(), Instant.now());
        redisTemplate.convertAndSend("kb:events", JSON.toJSONString(event));
    }
}

// 事件订阅（kb-knowledge 监听并更新索引）
@Service
public class IndexEventListener {
    @Autowired
    private MeiliSearchService searchService;

    @EventListener
    public void onMessage(String message) {
        KbEvent event = JSON.parseObject(message, KbEvent.class);
        switch (event.getEvent()) {
            case "file.parsed" -> searchService.addIndex("kb_files",
                event.getFileId(), event.getPayload().get("content"));
            case "file.deleted" -> searchService.removeIndex("kb_files",
                event.getFileId());
        }
    }
}
```

**第二层：定时补偿任务（每天 03:00 执行）**

```java
// kb-knowledge: ConsistencyCheckTask
@Service
public class ConsistencyCheckTask {

    /**
     * 每天 03:00 执行数据一致性校验
     * 1. 对比 kb_file 表和 MeiliSearch kb_files 索引，修复差异
     * 2. 对比 share 表和 file 表，标记失效分享
     * 3. 将修复记录写入 operation_log
     */
    @Scheduled(cron = "0 0 3 * * ?")
    public void consistencyCheck() {
        // 1. 索引一致性：MySQL 有但 MeiliSearch 没有的 → 补建索引
        List<Long> missingIndexIds = fileMapper.selectIdsNotInSearchIndex();
        missingIndexIds.forEach(id -> {
            File f = fileMapper.selectById(id);
            searchService.addIndex("kb_files", f.getId(), f.getParsedContent());
            log.info("[补偿] 补建索引 fileId={}", id);
        });

        // 2. 索引清理：MeiliSearch 有但 MySQL 已删除的 → 删索引
        List<Long> orphanIndexIds = searchService.selectOrphanIds("kb_files");
        orphanIndexIds.forEach(id -> {
            searchService.removeIndex("kb_files", id);
            log.info("[补偿] 清理孤儿索引 fileId={}", id);
        });

        // 3. 分享有效性：文件已删但分享还在的 → 标记失效
        shareMapper.markInvalidShares();

        log.info("[一致性检查] 完成，补建 {}，清理 {}，标记失效分享 {}",
            missingIndexIds.size(), orphanIndexIds.size(), ...);
    }
}
```

**补偿任务执行表：**

| 检查项 | 频率 | 异常处理 | 日志 |
|--------|------|---------|------|
| MySQL ↔ MeiliSearch 索引一致性 | 每天 03:00 | 自动补建/清理 | operation_log |
| 分享链接有效性 | 每天 03:00 | 标记失效 | operation_log |
| kb-ops 快照完整性 | 每天 03:00 | 重新统计 | ops_change_log |
| 文件-目录关联完整性 | 每周一 03:00 | 修复孤儿文件 | operation_log |

---

## P1 优化：统一日志格式

### Logback 配置（所有服务共用，放 kb-common）

```xml
<!-- kb-common: logback-spring.xml -->
<configuration>
    <springProperty scope="context" name="APP_NAME" source="spring.application.name"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{traceId:-}] %-5level ${APP_NAME} - %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>/data/logs/${APP_NAME}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>/data/logs/${APP_NAME}.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>5GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{traceId:-}] %-5level ${APP_NAME} - %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>
```

**日志格式示例：**
```
2026-06-20 14:30:22.456 [http-nio-8083-exec-1] [a1b2c3d4] INFO  kb-knowledge - c.k.k.controller.FileController - 文件上传成功 fileId=123
2026-06-20 14:30:23.789 [http-nio-8083-exec-2] [a1b2c3d4] WARN  kb-knowledge - c.k.k.service.IndexService - 索引写入重试 1/3 fileId=123
2026-06-20 14:30:24.012 [http-nio-8084-exec-1] [e5f6g7h8] ERROR kb-ops       - c.k.o.service.ImportService - 导入失败 host=内网Debian
```

**日志规范：**

| 级别 | 使用场景 | 示例 |
|------|---------|------|
| ERROR | 系统异常、不可恢复错误 | 数据库连接失败、文件解析异常 |
| WARN | 可恢复异常、业务警告 | 索引写入重试、Token 即将过期 |
| INFO | 关键业务节点 | 用户登录、文件上传、导入完成 |
| DEBUG | 调试信息（生产关闭） | SQL 执行、Feign 调用参数 |

**日志目录规划：**

| 服务 | 日志文件 | 轮转策略 |
|------|---------|---------|
| kb-gateway | `/data/logs/kb-gateway.log` | 50MB × 30天 |
| kb-auth | `/data/logs/kb-auth.log` | 50MB × 30天 |
| kb-file | `/data/logs/kb-file.log` | 50MB × 30天 |
| kb-knowledge | `/data/logs/kb-knowledge.log` | 50MB × 30天 |
| kb-ops | `/data/logs/kb-ops.log` | 50MB × 30天 |

**统一排查方式：**
```bash
# 按 traceId 跨服务搜索
grep "a1b2c3d4" /data/logs/kb-*.log

# 按时间范围搜索
awk '$1>="2026-06-20" && $1<="2026-06-21"' /data/logs/kb-*.log

# 按级别过滤
grep " ERROR " /data/logs/kb-*.log | tail -20
```

## 实施步骤

### Step 1: 创建父工程 + 通用模块
- kb-parent (pom.xml)
- kb-common (公共工具类、DTO、异常、统一返回)

### Step 2: 实现 kb-auth 服务
- 用户注册/登录
- JWT 双 Token
- API Token 管理（含 scope）
- CryptoUtil（AES-256-GCM）

### Step 3: 实现 kb-file 服务
- 分片上传（修复前后端不匹配）
- 文件解析（txt/md/pdf/docx + MeiliSearch 索引写入）
- MinIO 集成

### Step 4: 实现 kb-knowledge 服务
- 目录 CRUD
- 笔记 CRUD
- 网页收藏
- 全文搜索（修复索引名 + 总数计算）
- 分享功能
- 操作日志

### Step 5: 实现 kb-ops 服务
- 运维知识 CRUD（8 张 ops_* 表）
- 外部导入 API（含 JSON Schema 校验）
- 矛盾检测（6 种规则）
- 知识看板数据接口

### Step 6: 实现 kb-gateway 服务
- 路由配置
- JWT 鉴权过滤器
- 静态资源代理

### Step 7: 前端适配
- 更新 API 路径
- 新增知识看板页面（10 个 /ops/* 页面）
- 修复分片上传调用

### Step 8: Docker Compose + Nginx
- 多容器编排
- Nginx 配置更新
