# MyKNG 知识库平台 API 接口规范清单 v2

> **版本**：v2.4（剥离 kb-ops 模块，操作日志接口迁移至 kb-auth）
> **更新日期**：2026-07-03
> **网关地址**：`http://localhost:8090`
> **上下文路径**：`/kb`（可通过环境变量 `KB_CONTEXT` 配置）
> **接口总数**：77 个（kb-auth:13、kb-file:13、kb-knowledge:51）
> **维护说明**：本文件由架构组维护，新增/变更接口请同步更新本清单并升级版本号。

---

## 目录

- [1. 概述](#1-概述)
  - [1.1 系统架构](#11-系统架构)
  - [1.2 公网访问路径说明](#12-公网访问路径说明)
  - [1.3 认证机制](#13-认证机制)
  - [1.4 统一返回格式](#14-统一返回格式)
  - [1.5 错误码对照表](#15-错误码对照表)
- [2. kb-auth 服务（13 个接口）](#2-kb-auth-服务13-个接口)
- [3. kb-file 服务（13 个接口）](#3-kb-file-服务13-个接口)
- [4. kb-knowledge 服务（51 个接口）](#4-kb-knowledge-服务51-个接口)
- [附录 A. 数据模型速查](#附录-a-数据模型速查)
- [附录 B. 变更记录](#附录-b-变更记录)

---

## 1. 概述

### 1.1 系统架构

MyKNG 采用 **Spring Cloud Gateway 微服务架构**，由网关统一负责路由转发、JWT 鉴权、跨域处理、限流与链路追踪。后端拆分为 3 个业务服务：

| 服务 | 端口 | 职责 |
|------|------|------|
| `kb-gateway` | 8090 | API 网关，统一入口、鉴权、路由 |
| `kb-auth` | 8081 | 认证授权、用户管理、API Token 管理、操作日志 |
| `kb-file` | 8082 | 文件上传/合并/解析、对象存储桶管理 |
| `kb-knowledge` | 8083 | 文档、文件夹、空间、标签、搜索、分享、回收站、版本、网页收藏 |

```
                        ┌─────────────────────┐
   Client / Frontend ──►│   kb-gateway (:8090) │
                        │  /kb/api/**          │
                        └──────┬───────────────┘
               ┌───────────────┼───────────────┐
               ▼               ▼               ▼
        ┌──────────┐    ┌──────────┐    ┌────────────┐
        │ kb-auth  │    │ kb-file  │    │kb-knowledge│
        │  :8081   │    │  :8082   │    │   :8083    │
        └──────────┘    └──────────┘    └────────────┘
```

### 1.2 公网访问路径说明

所有对外接口统一通过网关暴露，公网路径格式为：

```
http://{网关地址}{上下文路径}/api/{module}/{action}
```

- **上下文路径**：`/kb`（默认值，由环境变量 `KB_CONTEXT` 控制，修改后所有路由前缀同步变更）
- **API 标识**：`/api`（固定，不可更改）
- **功能模块**：由 Controller 的 `@RequestMapping` 定义，如 `/auth`、`/file`、`/doc`、`/log` 等
- **StripPrefix=2**：网关转发时剥离 `/kb/api` 两段路径，后端服务收到的是 `/{module}/{action}`

> **示例**：客户端请求 `GET http://localhost:8090/kb/api/user/profile`
> 网关转发至 `http://localhost:8081/user/profile`（kb-auth 服务）

#### 路由映射表

| 网关路由 ID | 网关匹配路径（/kb/api 前缀下） | 转发目标 |
|-------------|-------------------------------|----------|
| kb-auth | `/auth/**`、`/user/**`、`/token/**`、`/log/**` | kb-auth:8081 |
| kb-file | `/file/**`、`/bucket/**` | kb-file:8082 |
| kb-knowledge | `/doc/**`、`/folder/**`、`/web/**`、`/search/**`、`/share/**`、`/tag/**`、`/space/**`、`/trash/**`、`/version/**` | kb-knowledge:8083 |

### 1.3 认证机制

#### 认证方式

采用 **JWT Bearer Token** 认证。客户端在请求头中携带 Access Token：

```
Authorization: Bearer <accessToken>
```

#### Token 类型

| Token 类型 | 用途 | 有效期 | 说明 |
|-----------|------|--------|------|
| **Access Token** | 访问业务接口 | 15 分钟（900000 毫秒） | JWT 格式，type=access |
| **Refresh Token** | 刷新 Access Token | 7 天（604800000 毫秒） | 用于 `/auth/refresh` 接口换取新的令牌对 |
| **API Token** | 第三方系统调用 | 默认 365 天，可永不过期 | 通过 `/token` 创建的长期令牌，明文仅返回一次 |

#### 鉴权流程

1. 网关 `JwtAuthFilter` 拦截所有 `/kb/api/**` 路径请求（白名单除外）
2. 从 `Authorization` 头提取 Bearer Token
3. 使用 HMAC-SHA 本地验签 + 校验过期 + 校验 `type=access`
4. 校验通过：清除客户端伪造的 `X-User-Id`，注入真实 `userId`/`username` 到下游请求头
5. 校验失败：返回 `401` 统一 JSON 响应（携带 `traceId`）

#### 白名单（免认证路径）

| 路径 | 说明 |
|------|------|
| `/kb/api/auth/login` | 用户登录 |
| `/kb/api/auth/refresh` | 刷新令牌 |
| `/kb/api/share/verify/**` | 分享验证（公开访问） |

> **注意**：`/kb/api/share/detail/{code}` 不在白名单中，需要认证才能访问。
> `/kb/api/token/verify` 在 kb-auth 的 SecurityConfig 中 `permitAll`，但仍经过网关 JWT 过滤器（建议生产环境补充白名单或在网关侧放行）。
> 所有 `OPTIONS` 预检请求（CORS）自动放行。

#### 令牌刷新机制

Access Token 过期后，客户端使用 Refresh Token 调用 `/auth/refresh` 获取新的令牌对。建议前端在检测到 401 响应时自动执行刷新流程。

### 1.4 统一返回格式

#### Result\<T\>（通用返回格式）

所有接口统一返回 `Result<T>` 结构：

```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "traceId": "a1b2c3d4e5f6"
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `code` | `int` | 状态码，`200` 表示成功，其余为错误码 |
| `message` | `string` | 状态描述信息 |
| `data` | `T` | 业务数据，失败时为 `null` |
| `traceId` | `string` | 链路追踪 ID，用于问题排查（由 `TraceIdFilter` 自动生成注入） |

#### PageResult\<T\>（分页返回格式）

分页接口的 `data` 字段为 `PageResult<T>`：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [],
    "total": 100,
    "page": 1,
    "size": 20
  },
  "traceId": "a1b2c3d4e5f6"
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `list` | `List<T>` | 当前页数据列表 |
| `total` | `long` | 总记录数 |
| `page` | `int` | 当前页码（从 1 开始） |
| `size` | `int` | 每页条数 |

### 1.5 错误码对照表

| 错误码 | HTTP Status | 说明 | 典型场景 |
|--------|-------------|------|----------|
| **200** | 200 | 成功 | 正常请求 |
| **400** | 400 | 参数校验失败 / 请求格式错误 / 业务异常 | 必填字段缺失、参数类型不匹配、导入数据为空、不支持的导入类型、业务规则违反（BusinessException 默认 code） |
| **401** | 401 | 未认证 / 认证失效 | 缺少 Token、Token 无效、Token 已过期、令牌类型错误、RefreshToken 无效、API Token 为空/过期、用户不存在或已禁用 |
| **403** | 403 | 无权限 | 无权操作他人资源（如他人创建的 Token） |
| **404** | 404 | 资源不存在 | 文件不存在、Bucket 不存在 |
| **409** | 409 | 资源冲突 | IP 已存在等唯一约束冲突 |
| **500** | 500 | 服务内部错误 | 未捕获异常 |

#### 401 响应示例

```json
{
  "code": 401,
  "message": "访问令牌无效或已过期",
  "data": null,
  "traceId": "a1b2c3d4e5f6"
}
```

#### 400 参数校验失败示例

```json
{
  "code": 400,
  "message": "参数校验失败: username: 用户名不能为空; password: 密码不能为空",
  "data": null,
  "traceId": "a1b2c3d4e5f6"
}
```

---

## 2. kb-auth 服务（13 个接口）

认证授权服务，负责用户登录、令牌管理、用户信息维护与操作日志记录。

> **默认账号**：`admin` / `admin123`（由数据库初始化脚本写入，首次登录后请及时修改密码）

### 2.1 用户登录

| 项目 | 说明 |
|------|------|
| **方法** | `POST` |
| **公网路径** | `/kb/api/auth/login` |
| **认证** | ❌ 否（白名单） |
| **描述** | 用户名密码登录，返回访问令牌与刷新令牌 |

**请求 Body**：
```json
{
  "username": "admin",
  "password": "admin123"
}
```

**响应 `data`**：
```json
{
  "accessToken": "eyJhbGciOi...",
  "refreshToken": "eyJhbGciOi...",
  "expiresIn": 900000
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `accessToken` | `String` | 访问令牌（JWT，有效期 15 分钟） |
| `refreshToken` | `String` | 刷新令牌（JWT，有效期 7 天） |
| `expiresIn` | `Long` | Access Token 过期时长（**毫秒**，默认 900000=15 分钟） |

---

### 2.2 用户登出

| 项目 | 说明 |
|------|------|
| **方法** | `POST` |
| **公网路径** | `/kb/api/auth/logout` |
| **认证** | ✅ 是 |
| **描述** | 退出登录，将当前 Access Token 加入黑名单 |

**请求头**：`Authorization: Bearer <accessToken>`

**响应 `data`**：`null`

---

### 2.3 刷新令牌

| 项目 | 说明 |
|------|------|
| **方法** | `POST` |
| **公网路径** | `/kb/api/auth/refresh` |
| **认证** | ❌ 否（白名单） |
| **描述** | 使用 Refresh Token 获取新的令牌对（旧 Refresh Token 失效） |

**请求 Body**：
```json
{
  "refreshToken": "eyJhbGciOi..."
}
```

**响应 `data`**：与登录响应结构相同（`expiresIn` 单位为毫秒）

---

### 2.4 获取用户信息

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/user/profile` |
| **认证** | ✅ 是 |
| **描述** | 获取当前登录用户的个人信息 |

**响应 `data`**：`User` 对象（见附录 A）

---

### 2.5 更新用户信息

| 项目 | 说明 |
|------|------|
| **方法** | `PUT` |
| **公网路径** | `/kb/api/user/profile` |
| **认证** | ✅ 是 |
| **描述** | 更新当前用户的个人资料（昵称/邮箱/手机/头像） |

**请求 Body**：
```json
{
  "nickname": "新昵称",
  "email": "new@example.com",
  "phone": "13900139000",
  "avatar": "/avatars/new.png"
}
```

**响应 `data`**：更新后的 `User` 对象

---

### 2.6 修改密码

| 项目 | 说明 |
|------|------|
| **方法** | `PUT` |
| **公网路径** | `/kb/api/user/password` |
| **认证** | ✅ 是 |
| **描述** | 修改当前用户密码 |

**请求 Body**：
```json
{
  "oldPassword": "admin123",
  "newPassword": "newPass456"
}
```

**响应 `data`**：`null`

---

### 2.7 创建 API Token

| 项目 | 说明 |
|------|------|
| **方法** | `POST` |
| **公网路径** | `/kb/api/token` |
| **认证** | ✅ 是 |
| **描述** | 创建 API Token。**明文 Token 仅在此接口返回一次，后续查询只能看到前缀。** |

**请求 Body**：
```json
{
  "name": "CI/CD 集成",
  "scope": "read,write",
  "expireAt": "2026-12-31 23:59:59"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | `String` | 是 | Token 名称 |
| `scope` | `String` | 否 | 权限范围，逗号分隔 |
| `expireAt` | `DateTime` | 否 | 过期时间（`yyyy-MM-dd HH:mm:ss`），为空表示永不过期 |

**响应 `data`**：`ApiTokenResponse` 对象（见附录 A）

---

### 2.8 API Token 列表

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/token` |
| **认证** | ✅ 是 |
| **描述** | 分页查询当前用户创建的 API Token 列表 |

**Query 参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page` | `int` | 否 | 页码，默认 1 |
| `size` | `int` | 否 | 每页条数，默认 20 |

**响应 `data`**：`PageResult<ApiToken>`（仅返回 tokenPrefix，不返回明文 token）

---

### 2.9 删除 API Token

| 项目 | 说明 |
|------|------|
| **方法** | `DELETE` |
| **公网路径** | `/kb/api/token/{id}` |
| **认证** | ✅ 是 |
| **描述** | 删除指定的 API Token（仅创建者可删） |

**路径参数**：`id`（Long）— Token ID

**响应 `data`**：`null`

---

### 2.10 启用/禁用 API Token

| 项目 | 说明 |
|------|------|
| **方法** | `PUT` |
| **公网路径** | `/kb/api/token/{id}/toggle` |
| **认证** | ✅ 是 |
| **描述** | 切换 API Token 的启用/禁用状态（仅创建者可操作） |

**路径参数**：`id`（Long）— Token ID

**响应 `data`**：`null`

---

### 2.11 验证 API Token

| 项目 | 说明 |
|------|------|
| **方法** | `POST` |
| **公网路径** | `/kb/api/token/verify` |
| **认证** | ❌ 否（kb-auth SecurityConfig permitAll） |
| **描述** | 验证 API Token 的有效性，返回关联的 Token 实体（供其他服务通过 Feign 调用） |

**请求 Body**：
```json
{
  "token": "kb_xxxxxxxxxxxxxxxxxxxxxxxx"
}
```

**响应 `data`**：`ApiToken` 对象（无效时抛出 BusinessException，返回 401）

---

### 2.12 获取当前用户信息（/auth/me）

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/auth/me` |
| **认证** | ✅ 是 |
| **描述** | 获取当前登录用户信息，返回完整的登录响应（含令牌和用户对象） |

**请求头**：`Authorization: Bearer <accessToken>`

**响应 `data`**：`LoginResponse` 对象（含 `user` 字段，见附录 A）

---

### 2.13 操作日志列表

> **迁移说明**：原属 kb-ops 模块（`/ops/log/list`），kb-ops 剥离后迁移至 kb-auth，路径改为 `/log/list`。

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/log/list` |
| **认证** | ✅ 是 |
| **描述** | 分页查询操作日志（由各服务通过 Redis Pub/Sub 异步上报） |

**Query 参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `userId` | `Long` | 否 | 用户 ID 筛选 |
| `action` | `String` | 否 | 操作类型筛选（如 `user.login`、`user.logout`） |
| `page` | `int` | 否 | 页码，默认 1 |
| `size` | `int` | 否 | 每页条数，默认 20 |

**响应 `data`**：`PageResult<OperationLog>`（见附录 A）

---

## 3. kb-file 服务（13 个接口）

文件管理服务，支持分片上传、合并、解析、下载及存储桶管理。

> **存储说明**：文件实体存储于 MinIO，元数据存于 MySQL `file` 表。文件内容解析（PDF/Word 等）由后台异步任务完成，解析结果存入 MongoDB 供全文检索。

### 3.1 文件分片上传

| 项目 | 说明 |
|------|------|
| **方法** | `POST` |
| **公网路径** | `/kb/api/file/upload` |
| **认证** | ✅ 是 |
| **描述** | 上传文件分片（multipart/form-data） |

**请求参数**（multipart）：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `file` | `MultipartFile` | 是 | 上传的文件分片 |
| `fileId` | `String` | 否 | 文件唯一标识（首次上传可空，后续分片需带上以关联） |
| `chunkNumber` | `Integer` | 否 | 当前分片序号（从 1 开始） |

**响应 `data`**：`String`（上传结果/分片标识）

---

### 3.2 文件分片合并

| 项目 | 说明 |
|------|------|
| **方法** | `POST` |
| **公网路径** | `/kb/api/file/merge` |
| **认证** | ✅ 是 |
| **描述** | 将已上传的分片合并为完整文件，触发解析流程 |

**请求 Body**：
```json
{
  "fileId": "abc-123-xyz",
  "name": "document.pdf",
  "folderId": 1,
  "size": 1048576,
  "totalChunks": 5
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `fileId` | `String` | 是 | 上传时使用的文件标识 |
| `name` | `String` | 是 | 文件名 |
| `folderId` | `Long` | 是 | 目标文件夹 ID |
| `size` | `Long` | 否 | 文件大小（字节） |
| `totalChunks` | `Integer` | 否 | 总分片数 |

**响应 `data`**：`KbFile` 对象（见附录 A）

---

### 3.3 文件列表查询

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/file/list` |
| **认证** | ✅ 是 |
| **描述** | 分页查询当前用户的文件列表 |

**Query 参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `folderId` | `Long` | 否 | 文件夹 ID 筛选 |
| `page` | `int` | 否 | 页码，默认 1 |
| `size` | `int` | 否 | 每页条数，默认 20 |

**响应 `data`**：`PageResult<KbFile>`

---

### 3.4 获取文件详情

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/file/{id}` |
| **认证** | ✅ 是 |
| **描述** | 获取指定文件的详细信息 |

**路径参数**：`id`（Long）— 文件 ID

**响应 `data`**：`KbFile` 对象

---

### 3.5 查询文件解析状态

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/file/{id}/parse-status` |
| **认证** | ✅ 是 |
| **描述** | 查询文件解析状态 |

**路径参数**：`id`（Long）— 文件 ID

**响应 `data`**：`String`（解析状态值，如 `PENDING`、`PROCESSING`、`SUCCESS`、`FAILED`）

---

### 3.6 获取文件解析内容

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/file/{id}/content` |
| **认证** | ✅ 是 |
| **描述** | 获取文件解析后的文本内容（从 MongoDB 读取） |

**路径参数**：`id`（Long）— 文件 ID

**响应 `data`**：`String`（文件解析后的纯文本内容）

---

### 3.7 下载文件

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/file/{id}/download` |
| **认证** | ✅ 是 |
| **描述** | 获取文件的预签名下载 URL（MinIO 直连） |

**路径参数**：`id`（Long）— 文件 ID

**响应 `data`**：`String`（预签名下载 URL，客户端直接访问该 URL 下载文件）

---

### 3.8 重新解析文件

| 项目 | 说明 |
|------|------|
| **方法** | `POST` |
| **公网路径** | `/kb/api/file/{id}/reparse` |
| **认证** | ✅ 是 |
| **描述** | 触发文件重新解析 |

**路径参数**：`id`（Long）— 文件 ID

**响应 `data`**：`null`

---

### 3.9 删除文件

| 项目 | 说明 |
|------|------|
| **方法** | `DELETE` |
| **公网路径** | `/kb/api/file/{id}` |
| **认证** | ✅ 是 |
| **描述** | 删除文件（逻辑删除，移入回收站） |

**路径参数**：`id`（Long）— 文件 ID

**响应 `data`**：`null`

---

### 3.10 文件收藏/取消收藏

| 项目 | 说明 |
|------|------|
| **方法** | `PUT` |
| **公网路径** | `/kb/api/file/{id}/star` |
| **认证** | ✅ 是 |
| **描述** | 切换文件收藏状态（无需请求体，调用即切换当前状态） |

**路径参数**：`id`（Long）— 文件 ID

**请求 Body**：无

**响应 `data`**：`null`

---

### 3.11 移动文件

| 项目 | 说明 |
|------|------|
| **方法** | `PUT` |
| **公网路径** | `/kb/api/file/{id}/move` |
| **认证** | ✅ 是 |
| **描述** | 将文件移动到指定文件夹 |

**路径参数**：`id`（Long）— 文件 ID

**请求 Body**：
```json
{
  "folderId": 2
}
```

**响应 `data`**：`null`

---

### 3.12 存储桶列表

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/bucket/list` |
| **认证** | ✅ 是 |
| **描述** | 获取所有存储桶列表 |

**响应 `data`**：`List<Bucket>`（见附录 A）

---

### 3.13 存储桶统计

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/bucket/{id}/stats` |
| **认证** | ✅ 是 |
| **描述** | 获取指定存储桶的使用统计 |

**路径参数**：`id`（Long）— 桶 ID

**响应 `data`**：`Map<String, Object>`（含 `fileCount`、`totalSize`、`usedPercent` 等字段）

---

## 4. kb-knowledge 服务（51 个接口）

知识库核心服务，涵盖文档管理、文件夹、空间、标签、搜索、分享、回收站、版本控制和网页收藏。

### 4.1 文档管理（7 个接口）

#### 4.1.1 文档列表

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/doc/list` |
| **认证** | ✅ 是 |
| **描述** | 分页查询当前用户的文档列表 |

**Query 参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `folderId` | `Long` | 否 | 文件夹 ID 筛选 |
| `page` | `int` | 否 | 页码，默认 1 |
| `size` | `int` | 否 | 每页条数，默认 20 |

**响应 `data`**：`PageResult<Doc>`

---

#### 4.1.2 创建文档

| 项目 | 说明 |
|------|------|
| **方法** | `POST` |
| **公网路径** | `/kb/api/doc` |
| **认证** | ✅ 是 |
| **描述** | 创建新文档（内容存入 MongoDB，元数据存入 MySQL） |

**请求 Body**：
```json
{
  "folderId": 1,
  "title": "文档标题",
  "content": "文档内容（Markdown）"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `folderId` | `Long` | 是 | 所属文件夹 ID |
| `title` | `String` | 是 | 文档标题 |
| `content` | `String` | 否 | 文档内容（存入 MongoDB，不存 MySQL） |

**响应 `data`**：`Doc` 对象（元数据，不含 content）

---

#### 4.1.3 获取文档详情

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/doc/{id}` |
| **认证** | ✅ 是 |
| **描述** | 获取指定文档的元数据（content 需另行从 MongoDB 查询） |

**路径参数**：`id`（Long）— 文档 ID

**响应 `data`**：`Doc` 对象

---

#### 4.1.4 更新文档

| 项目 | 说明 |
|------|------|
| **方法** | `PUT` |
| **公网路径** | `/kb/api/doc/{id}` |
| **认证** | ✅ 是 |
| **描述** | 更新文档标题和内容（生成新版本） |

**路径参数**：`id`（Long）— 文档 ID

**请求 Body**：
```json
{
  "title": "更新后的标题",
  "content": "更新后的内容"
}
```

**响应 `data`**：`Doc` 对象

---

#### 4.1.5 删除文档

| 项目 | 说明 |
|------|------|
| **方法** | `DELETE` |
| **公网路径** | `/kb/api/doc/{id}` |
| **认证** | ✅ 是 |
| **描述** | 删除文档（逻辑删除，移入回收站） |

**路径参数**：`id`（Long）— 文档 ID

**响应 `data`**：`null`

---

#### 4.1.6 文档收藏/取消收藏

| 项目 | 说明 |
|------|------|
| **方法** | `PUT` |
| **公网路径** | `/kb/api/doc/{id}/star` |
| **认证** | ✅ 是 |
| **描述** | 切换文档收藏状态（无需请求体，调用即切换当前状态） |

**路径参数**：`id`（Long）— 文档 ID

**请求 Body**：无

**响应 `data`**：`null`

---

#### 4.1.7 移动文档

| 项目 | 说明 |
|------|------|
| **方法** | `PUT` |
| **公网路径** | `/kb/api/doc/{id}/move` |
| **认证** | ✅ 是 |
| **描述** | 将文档移动到指定文件夹 |

**路径参数**：`id`（Long）— 文档 ID

**请求 Body**：
```json
{
  "folderId": 2
}
```

**响应 `data`**：`null`

---

### 4.2 文件夹管理（8 个接口）

#### 4.2.1 获取文件夹树（路径参数方式）

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/folder/tree/{spaceId}` |
| **认证** | ✅ 是 |
| **描述** | 获取指定空间的文件夹树形结构（路径参数方式） |

**路径参数**：`spaceId`（Long）— 空间 ID

**响应 `data`**：`List<Folder>`（树形结构，含 `children` 字段）

---

#### 4.2.2 获取文件夹树（查询参数方式）

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/folder/tree?spaceId=xxx` |
| **认证** | ✅ 是 |
| **描述** | 获取指定空间的文件夹树形结构（查询参数方式，与路径参数方式等价） |

**Query 参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `spaceId` | `Long` | 是 | 空间 ID |

**响应 `data`**：`List<Folder>`（树形结构，含 `children` 字段）

---

#### 4.2.3 获取文件夹详情

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/folder/{id}` |
| **认证** | ✅ 是 |
| **描述** | 获取指定文件夹的详细信息 |

**路径参数**：`id`（Long）— 文件夹 ID

**响应 `data`**：`Folder` 对象

---

#### 4.2.4 创建文件夹

| 项目 | 说明 |
|------|------|
| **方法** | `POST` |
| **公网路径** | `/kb/api/folder` |
| **认证** | ✅ 是 |
| **描述** | 创建新文件夹 |

**请求 Body**：
```json
{
  "spaceId": 1,
  "parentId": 0,
  "name": "新文件夹",
  "sortOrder": 1
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `spaceId` | `Long` | 是 | 所属空间 ID |
| `parentId` | `Long` | 否 | 父文件夹 ID（根目录传 0 或不传） |
| `name` | `String` | 是 | 文件夹名称 |
| `sortOrder` | `Integer` | 否 | 排序序号 |

**响应 `data`**：`Folder` 对象

---

#### 4.2.5 重命名文件夹

| 项目 | 说明 |
|------|------|
| **方法** | `PUT` |
| **公网路径** | `/kb/api/folder/{id}` |
| **认证** | ✅ 是 |
| **描述** | 重命名指定文件夹 |

**路径参数**：`id`（Long）— 文件夹 ID

**请求 Body**：
```json
{
  "name": "新名称"
}
```

**响应 `data`**：`Folder` 对象

---

#### 4.2.6 删除文件夹

| 项目 | 说明 |
|------|------|
| **方法** | `DELETE` |
| **公网路径** | `/kb/api/folder/{id}` |
| **认证** | ✅ 是 |
| **描述** | 删除指定文件夹 |

**路径参数**：`id`（Long）— 文件夹 ID

**响应 `data`**：`null`

---

#### 4.2.7 移动文件夹

| 项目 | 说明 |
|------|------|
| **方法** | `PUT` |
| **公网路径** | `/kb/api/folder/{id}/move` |
| **认证** | ✅ 是 |
| **描述** | 将文件夹移动到指定父文件夹下 |

**路径参数**：`id`（Long）— 文件夹 ID

**请求 Body**：
```json
{
  "parentId": 2
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `parentId` | `Long` | 是 | 目标父文件夹 ID（根目录传 0） |

**响应 `data`**：`null`

---

#### 4.2.8 文件夹排序

| 项目 | 说明 |
|------|------|
| **方法** | `PUT` |
| **公网路径** | `/kb/api/folder/{id}/sort` |
| **认证** | ✅ 是 |
| **描述** | 调整文件夹排序顺序 |

**路径参数**：`id`（Long）— 文件夹 ID

**请求 Body**：
```json
{
  "sortOrder": 1
}
```

**响应 `data`**：`null`

---

### 4.3 搜索（1 个接口）

#### 4.3.1 全局搜索

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/search` |
| **认证** | ✅ 是 |
| **描述** | 全文搜索知识库内容（基于 MeiliSearch），返回分页结果 |

**Query 参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `q` | `String` | 是 | 搜索关键词 |
| `type` | `String` | 否 | 内容类型筛选（DOC/FILE/WEB） |
| `folderId` | `Long` | 否 | 限定文件夹 |
| `tagId` | `Long` | 否 | 限定标签 |
| `page` | `int` | 否 | 页码，默认 1 |
| `size` | `int` | 否 | 每页条数，默认 20 |

**响应 `data`**：`PageResult<Map<String, Object>>`

---

### 4.4 分享管理（6 个接口）

#### 4.4.1 创建分享

| 项目 | 说明 |
|------|------|
| **方法** | `POST` |
| **公网路径** | `/kb/api/share` |
| **认证** | ✅ 是 |
| **描述** | 创建分享链接 |

**请求 Body**：
```json
{
  "resourceType": "doc",
  "resourceId": 1,
  "extractCode": "a1b2",
  "expireAt": "2026-12-31T23:59:59"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `resourceType` | `String` | 是 | 资源类型（`doc`、`file`、`web`、`folder`，小写） |
| `resourceId` | `Long` | 是 | 资源 ID |
| `extractCode` | `String` | 否 | 提取码（为空表示无需提取码，服务端自动生成） |
| `expireAt` | `String` | 否 | 过期时间（支持 `yyyy-MM-ddTHH:mm:ss`（ISO，推荐）和 `yyyy-MM-dd HH:mm:ss` 两种格式，为空表示永不过期） |

**响应 `data`**：`Share` 对象（见附录 A）

---

#### 4.4.2 分享列表

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/share/list` |
| **认证** | ✅ 是 |
| **描述** | 分页查询当前用户创建的分享列表 |

**Query 参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page` | `int` | 否 | 页码，默认 1 |
| `size` | `int` | 否 | 每页条数，默认 20 |

**响应 `data`**：`PageResult<Share>`

---

#### 4.4.3 我的分享列表

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/share/my` |
| **认证** | ✅ 是 |
| **描述** | 获取当前用户创建的所有分享列表（不分页，返回完整列表） |

**响应 `data`**：`List<Share>`

---

#### 4.4.4 删除分享

| 项目 | 说明 |
|------|------|
| **方法** | `DELETE` |
| **公网路径** | `/kb/api/share/{id}` |
| **认证** | ✅ 是 |
| **描述** | 撤销指定分享（仅创建者可删） |

**路径参数**：`id`（Long）— 分享 ID

**响应 `data`**：`null`

---

#### 4.4.5 验证分享

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/share/verify/{code}` |
| **认证** | ❌ 否（白名单） |
| **描述** | 验证分享码与提取码，返回分享基本信息（用于公开访问页） |

**路径参数**：`code`（String）— 分享码

**Query 参数**：`extractCode`（String）— 提取码（若分享设置了提取码则必填）

**响应 `data`**：`Share` 对象（分享元信息，不含实际内容）

---

#### 4.4.6 获取分享内容详情

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/share/detail/{code}` |
| **认证** | ✅ 是 |
| **描述** | 验证通过后获取分享的实际内容详情 |

> **重要**：此接口**不在白名单中**，需要认证才能访问。生产环境若需公开访问，应在网关白名单中补充 `/kb/api/share/detail/**`，或由后端服务自行校验 extractCode。

**路径参数**：`code`（String）— 分享码

**Query 参数**：`extractCode`（String）— 提取码

**响应 `data`**：分享内容详情（文档内容等）

---

### 4.5 空间管理（5 个接口）

#### 4.5.1 空间列表

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/space/list` |
| **认证** | ✅ 是 |
| **描述** | 获取当前用户可访问的知识空间列表 |

**响应 `data`**：`List<Space>`（见附录 A）

---

#### 4.5.2 获取空间详情

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/space/{id}` |
| **认证** | ✅ 是 |
| **描述** | 获取指定知识空间的详细信息 |

**路径参数**：`id`（Long）— 空间 ID

**响应 `data`**：`Space` 对象

---

#### 4.5.3 创建空间

| 项目 | 说明 |
|------|------|
| **方法** | `POST` |
| **公网路径** | `/kb/api/space` |
| **认证** | ✅ 是 |
| **描述** | 创建新的知识空间 |

**请求 Body**：
```json
{
  "name": "团队空间",
  "type": "TEAM",
  "description": "团队共享知识库"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | `String` | 是 | 空间名称 |
| `type` | `String` | 否 | 类型（PERSONAL、TEAM 等） |
| `description` | `String` | 否 | 描述 |

**响应 `data`**：`Space` 对象

---

#### 4.5.4 更新空间

| 项目 | 说明 |
|------|------|
| **方法** | `PUT` |
| **公网路径** | `/kb/api/space/{id}` |
| **认证** | ✅ 是 |
| **描述** | 更新空间名称、类型、描述或状态 |

**路径参数**：`id`（Long）— 空间 ID

**请求 Body**：
```json
{
  "name": "更新后的名称",
  "type": "TEAM",
  "description": "更新后的描述",
  "status": 1
}
```

**响应 `data`**：`Space` 对象

---

#### 4.5.5 删除空间

| 项目 | 说明 |
|------|------|
| **方法** | `DELETE` |
| **公网路径** | `/kb/api/space/{id}` |
| **认证** | ✅ 是 |
| **描述** | 删除指定知识空间 |

**路径参数**：`id`（Long）— 空间 ID

**响应 `data`**：`null`

---

### 4.6 标签管理（10 个接口）

#### 4.6.1 标签列表

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/tag/list` |
| **认证** | ✅ 是 |
| **描述** | 获取当前用户的所有标签列表 |

**响应 `data`**：`List<Tag>`（见附录 A）

---

#### 4.6.2 创建标签

| 项目 | 说明 |
|------|------|
| **方法** | `POST` |
| **公网路径** | `/kb/api/tag` |
| **认证** | ✅ 是 |
| **描述** | 创建新标签 |

**请求 Body**：
```json
{
  "name": "待办",
  "color": "#0000FF"
}
```

**响应 `data`**：`Tag` 对象

---

#### 4.6.3 获取标签详情

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/tag/{id}` |
| **认证** | ✅ 是 |
| **描述** | 获取指定标签的详细信息 |

**路径参数**：`id`（Long）— 标签 ID

**响应 `data`**：`Tag` 对象

---

#### 4.6.4 更新标签

| 项目 | 说明 |
|------|------|
| **方法** | `PUT` |
| **公网路径** | `/kb/api/tag/{id}` |
| **认证** | ✅ 是 |
| **描述** | 更新标签名称和颜色 |

**路径参数**：`id`（Long）— 标签 ID

**请求 Body**：
```json
{
  "name": "已完成",
  "color": "#00FF00"
}
```

**响应 `data`**：`Tag` 对象

---

#### 4.6.5 删除标签

| 项目 | 说明 |
|------|------|
| **方法** | `DELETE` |
| **公网路径** | `/kb/api/tag/{id}` |
| **认证** | ✅ 是 |
| **描述** | 删除指定标签 |

**路径参数**：`id`（Long）— 标签 ID

**响应 `data`**：`null`

---

#### 4.6.6 获取资源标签列表（推荐接口）

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/tag/resource?resourceId=&resourceType=` |
| **认证** | ✅ 是 |
| **描述** | 获取指定资源绑定的标签列表（推荐使用） |

**Query 参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `resourceId` | `Long` | 是 | 资源 ID |
| `resourceType` | `String` | 是 | 资源类型（DOC、FILE、WEB） |

**响应 `data`**：`List<Tag>`

---

#### 4.6.7 绑定标签到资源（推荐接口）

| 项目 | 说明 |
|------|------|
| **方法** | `POST` |
| **公网路径** | `/kb/api/tag/resource` |
| **认证** | ✅ 是 |
| **描述** | 将标签绑定到指定资源（推荐使用） |

**请求 Body**：
```json
{
  "tagId": 1,
  "resourceType": "DOC",
  "resourceId": 1
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `tagId` | `Long` | 是 | 标签 ID |
| `resourceType` | `String` | 是 | 资源类型（DOC、FILE、WEB） |
| `resourceId` | `Long` | 是 | 资源 ID |

**响应 `data`**：`null`

---

#### 4.6.8 解绑标签（推荐接口）

| 项目 | 说明 |
|------|------|
| **方法** | `DELETE` |
| **公网路径** | `/kb/api/tag/resource` |
| **认证** | ✅ 是 |
| **描述** | 解除标签与资源的绑定关系（推荐使用） |

**请求 Body**：
```json
{
  "tagId": 1,
  "resourceType": "DOC",
  "resourceId": 1
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `tagId` | `Long` | 是 | 标签 ID |
| `resourceType` | `String` | 是 | 资源类型（DOC、FILE、WEB） |
| `resourceId` | `Long` | 是 | 资源 ID |

**响应 `data`**：`null`

---

#### 4.6.9 绑定标签（兼容旧接口）

| 项目 | 说明 |
|------|------|
| **方法** | `POST` |
| **公网路径** | `/kb/api/tag/bind` |
| **认证** | ✅ 是 |
| **描述** | 将标签绑定到指定资源（文档/文件/网页等）。**兼容旧接口，推荐使用 `/tag/resource`** |

**请求 Body**：
```json
{
  "tagId": 1,
  "resourceType": "DOC",
  "resourceId": 1
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `tagId` | `Long` | 是 | 标签 ID |
| `resourceType` | `String` | 是 | 资源类型（DOC、FILE、WEB） |
| `resourceId` | `Long` | 是 | 资源 ID |

**响应 `data`**：`null`

---

#### 4.6.10 解绑标签（兼容旧接口）

| 项目 | 说明 |
|------|------|
| **方法** | `DELETE` |
| **公网路径** | `/kb/api/tag/unbind` |
| **认证** | ✅ 是 |
| **描述** | 解除标签与资源的绑定关系。**兼容旧接口，推荐使用 `/tag/resource`** |

**Query 参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `tagId` | `Long` | 是 | 标签 ID |
| `resourceType` | `String` | 是 | 资源类型 |
| `resourceId` | `Long` | 是 | 资源 ID |

**响应 `data`**：`null`

---

### 4.7 回收站（4 个接口）

#### 4.7.1 回收站列表

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/trash/list` |
| **认证** | ✅ 是 |
| **描述** | 分页查询当前用户的回收站内容 |

**Query 参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `type` | `String` | 否 | 内容类型筛选（DOC、FILE、WEB） |
| `page` | `int` | 否 | 页码，默认 1 |
| `size` | `int` | 否 | 每页条数，默认 20 |

**响应 `data`**：`PageResult<Map<String, Object>>`

---

#### 4.7.2 恢复内容

| 项目 | 说明 |
|------|------|
| **方法** | `POST` |
| **公网路径** | `/kb/api/trash/restore/{type}/{id}` |
| **认证** | ✅ 是 |
| **描述** | 从回收站恢复指定内容到原位置 |

**路径参数**：

| 参数 | 类型 | 说明 |
|------|------|------|
| `type` | `String` | 内容类型（DOC、FILE、WEB） |
| `id` | `Long` | 内容 ID |

**响应 `data`**：`null`

---

#### 4.7.3 彻底删除

| 项目 | 说明 |
|------|------|
| **方法** | `DELETE` |
| **公网路径** | `/kb/api/trash/{type}/{id}` |
| **认证** | ✅ 是 |
| **描述** | 从回收站永久删除指定内容（不可恢复） |

**路径参数**：

| 参数 | 类型 | 说明 |
|------|------|------|
| `type` | `String` | 内容类型 |
| `id` | `Long` | 内容 ID |

**响应 `data`**：`null`

---

#### 4.7.4 清空回收站

| 项目 | 说明 |
|------|------|
| **方法** | `DELETE` |
| **公网路径** | `/kb/api/trash/empty` |
| **认证** | ✅ 是 |
| **描述** | 清空回收站所有内容（不可恢复） |

**请求 Body**：无

**响应 `data`**：`null`

---

### 4.8 版本管理（3 个接口）

#### 4.8.1 版本历史列表

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径（路径参数方式）** | `/kb/api/version/list/{type}/{id}` |
| **公网路径（查询参数方式）** | `/kb/api/version/list?resourceType=&resourceId=` |
| **认证** | ✅ 是 |
| **描述** | 获取指定资源的版本历史列表，支持路径参数和查询参数两种方式 |

**路径参数**：

| 参数 | 类型 | 说明 |
|------|------|------|
| `type` | `String` | 资源类型（DOC、WEB 等） |
| `id` | `Long` | 资源 ID |

**Query 参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `resourceType` | `String` | 是 | 资源类型（DOC、WEB 等），查询参数方式使用 |
| `resourceId` | `Long` | 是 | 资源 ID，查询参数方式使用 |

**响应 `data`**：`List<Version>`（见附录 A）

---

#### 4.8.2 获取指定版本

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/version/{id}` |
| **认证** | ✅ 是 |
| **描述** | 获取指定版本的详细信息 |

**路径参数**：`id`（Long）— 版本 ID

**响应 `data`**：`Version` 对象

---

#### 4.8.3 回滚到指定版本

| 项目 | 说明 |
|------|------|
| **方法** | `POST` |
| **公网路径** | `/kb/api/version/{id}/rollback` |
| **认证** | ✅ 是 |
| **描述** | 将内容回滚到指定历史版本（生成新版本） |

**路径参数**：`id`（Long）— 版本 ID

**响应 `data`**：`Version` 对象（回滚后生成的新版本）

---

### 4.9 网页收藏（7 个接口）

#### 4.9.1 收藏网页

| 项目 | 说明 |
|------|------|
| **方法** | `POST` |
| **公网路径** | `/kb/api/web/collect` |
| **认证** | ✅ 是 |
| **描述** | 收藏网页，自动抓取网页内容并存储 |

**请求 Body**：
```json
{
  "url": "https://example.com/article",
  "folderId": 1
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `url` | `String` | 是 | 网页 URL |
| `folderId` | `Long` | 是 | 所属文件夹 ID |

**响应 `data`**：`WebPage` 对象（见附录 A）

---

#### 4.9.2 网页列表

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/web/list` |
| **认证** | ✅ 是 |
| **描述** | 分页查询当前用户收藏的网页列表 |

**Query 参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `folderId` | `Long` | 否 | 文件夹 ID 筛选 |
| `page` | `int` | 否 | 页码，默认 1 |
| `size` | `int` | 否 | 每页条数，默认 20 |

**响应 `data`**：`PageResult<WebPage>`

---

#### 4.9.3 获取网页详情

| 项目 | 说明 |
|------|------|
| **方法** | `GET` |
| **公网路径** | `/kb/api/web/{id}` |
| **认证** | ✅ 是 |
| **描述** | 获取收藏的网页详情 |

**路径参数**：`id`（Long）— 网页 ID

**响应 `data`**：`WebPage` 对象

---

#### 4.9.4 删除网页收藏

| 项目 | 说明 |
|------|------|
| **方法** | `DELETE` |
| **公网路径** | `/kb/api/web/{id}` |
| **认证** | ✅ 是 |
| **描述** | 删除收藏的网页（逻辑删除，移入回收站） |

**路径参数**：`id`（Long）— 网页 ID

**响应 `data`**：`null`

---

#### 4.9.5 网页收藏/取消收藏

| 项目 | 说明 |
|------|------|
| **方法** | `PUT` |
| **公网路径** | `/kb/api/web/{id}/star` |
| **认证** | ✅ 是 |
| **描述** | 切换网页收藏状态（无需请求体） |

**路径参数**：`id`（Long）— 网页 ID

**请求 Body**：无

**响应 `data`**：`null`

---

#### 4.9.6 移动网页

| 项目 | 说明 |
|------|------|
| **方法** | `PUT` |
| **公网路径** | `/kb/api/web/{id}/move` |
| **认证** | ✅ 是 |
| **描述** | 将网页移动到指定文件夹 |

**路径参数**：`id`（Long）— 网页 ID

**请求 Body**：
```json
{
  "folderId": 2
}
```

**响应 `data`**：`null`

---

#### 4.9.7 重新抓取网页

| 项目 | 说明 |
|------|------|
| **方法** | `POST` |
| **公网路径** | `/kb/api/web/{id}/refetch` |
| **认证** | ✅ 是 |
| **描述** | 重新抓取网页内容并更新快照 |

**路径参数**：`id`（Long）— 网页 ID

**响应 `data`**：`WebPage` 对象（更新后的网页信息）

---

## 附录 A. 数据模型速查

> 以下为接口中涉及的核心数据模型字段概览，具体字段以实际数据库实体为准。

### User

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `Long` | 用户 ID |
| `username` | `String` | 用户名 |
| `email` | `String` | 邮箱 |
| `phone` | `String` | 手机号 |
| `nickname` | `String` | 昵称 |
| `avatar` | `String` | 头像路径 |
| `status` | `Integer` | 状态（1=正常, 0=禁用） |

### LoginResponse

| 字段 | 类型 | 说明 |
|------|------|------|
| `accessToken` | `String` | 访问令牌 |
| `refreshToken` | `String` | 刷新令牌 |
| `expiresIn` | `Long` | Access Token 过期时长（**毫秒**，默认 900000） |
| `user` | `User` | 当前用户信息对象（仅 `/auth/me` 接口返回） |

### ApiToken

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `Long` | Token ID |
| `userId` | `Long` | 创建者用户 ID |
| `name` | `String` | Token 名称 |
| `tokenPrefix` | `String` | Token 前缀（仅显示前几位） |
| `scope` | `String` | 权限范围 |
| `status` | `Integer` | 状态（1=启用, 0=禁用） |
| `expireAt` | `DateTime` | 过期时间 |
| `createdAt` | `DateTime` | 创建时间 |

### ApiTokenResponse（创建时返回）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `Long` | Token ID |
| `name` | `String` | Token 名称 |
| `token` | `String` | 明文 Token（**仅创建时返回一次**，格式 `kb_xxxx`） |
| `tokenPrefix` | `String` | Token 前缀 |
| `scope` | `String` | 权限范围 |
| `expireAt` | `DateTime` | 过期时间 |
| `createdAt` | `DateTime` | 创建时间 |

### KbFile

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `Long` | 文件 ID |
| `folderId` | `Long` | 所属文件夹 ID |
| `userId` | `Long` | 上传用户 ID |
| `name` | `String` | 文件名 |
| `type` | `String` | 文件类型 |
| `size` | `Long` | 文件大小（字节） |
| `minioPath` | `String` | MinIO 存储路径 |
| `parseStatus` | `String` | 解析状态（PENDING/PROCESSING/SUCCESS/FAILED） |
| `parseError` | `String` | 解析错误信息 |
| `starred` | `Integer` | 是否收藏（1=是, 0=否） |
| `createdAt` | `DateTime` | 创建时间 |
| `updatedAt` | `DateTime` | 更新时间 |

### Doc

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `Long` | 文档 ID |
| `folderId` | `Long` | 所属文件夹 ID |
| `userId` | `Long` | 创建者用户 ID |
| `title` | `String` | 标题 |
| `starred` | `Integer` | 是否收藏（1=是, 0=否） |
| `createdAt` | `DateTime` | 创建时间 |
| `updatedAt` | `DateTime` | 更新时间 |

> **注**：文档 `content` 不在 MySQL 实体中，存储于 MongoDB，通过 `/doc/{id}` 详情接口或搜索接口返回。

### Space

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `Long` | 空间 ID |
| `name` | `String` | 空间名称 |
| `description` | `String` | 描述 |
| `type` | `String` | 类型（PERSONAL、TEAM 等） |
| `status` | `Integer` | 状态 |

### Folder

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `Long` | 文件夹 ID |
| `spaceId` | `Long` | 所属空间 ID |
| `parentId` | `Long` | 父文件夹 ID（根目录为 0） |
| `name` | `String` | 文件夹名称 |
| `sortOrder` | `Integer` | 排序序号 |
| `children` | `List<Folder>` | 子文件夹（树形结构时返回） |

### Tag

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `Long` | 标签 ID |
| `name` | `String` | 标签名称 |
| `color` | `String` | 标签颜色（十六进制） |

### Share

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `Long` | 分享 ID |
| `userId` | `Long` | 创建者用户 ID |
| `resourceType` | `String` | 资源类型（DOC、FILE、WEB） |
| `resourceId` | `Long` | 资源 ID |
| `code` | `String` | 分享码 |
| `extractCode` | `String` | 提取码 |
| `expireAt` | `DateTime` | 过期时间 |
| `viewCount` | `Integer` | 查看次数 |
| `createdAt` | `DateTime` | 创建时间 |
| `updatedAt` | `DateTime` | 更新时间 |

### WebPage

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `Long` | 网页 ID |
| `folderId` | `Long` | 所属文件夹 ID |
| `userId` | `Long` | 收藏用户 ID |
| `url` | `String` | 原始 URL |
| `title` | `String` | 网页标题 |
| `snapshotPath` | `String` | 快照存储路径 |
| `starred` | `Integer` | 是否收藏（1=是, 0=否） |
| `createdAt` | `DateTime` | 创建时间 |
| `updatedAt` | `DateTime` | 更新时间 |

### Version

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `Long` | 版本 ID |
| `resourceType` | `String` | 资源类型（DOC、WEB 等） |
| `resourceId` | `Long` | 资源 ID |
| `versionNum` | `Integer` | 版本号 |
| `createdAt` | `DateTime` | 创建时间 |

### Bucket

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `Long` | 桶 ID |
| `name` | `String` | 桶名称 |
| `type` | `String` | 存储类型（local、s3 等） |
| `capacity` | `Long` | 总容量（字节） |
| `used` | `Long` | 已用容量（字节） |

### OperationLog

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `Long` | 日志 ID |
| `userId` | `Long` | 用户 ID |
| `username` | `String` | 用户名 |
| `action` | `String` | 操作类型（如 `user.login`、`doc.create`） |
| `resourceType` | `String` | 资源类型 |
| `resourceId` | `Long` | 资源 ID |
| `detail` | `String` | 详情 |
| `ip` | `String` | 客户端 IP |
| `userAgent` | `String` | User-Agent |
| `createdAt` | `DateTime` | 创建时间 |

---

## 附录 B. 变更记录

| 版本 | 日期 | 变更内容 | 维护人 |
|------|------|----------|--------|
| v1.0 | 2026-06-21 | 初始版本，覆盖 83 个接口 | 架构组 |
| v2.0 | 2026-06-26 | 基于源码逐一核验重写：修正 30+ 处字段/参数不一致；补充 12 个遗漏接口（doc/list、tag/unbind、web/list、web/move、web/refetch、token 列表、log/list 等）；修正 expiresIn 单位为毫秒；修正分享/移动请求体字段；更新白名单说明；补全附录 A 数据模型 | 架构组 |
| v2.1 | 2026-06-27 | 修复 OperationLogController 路径不匹配（统一为 `/ops/log`）并补充操作日志详情接口；kb-ops 接口数由 26 更新为 27，总数 96 | 架构组 |
| v2.2 | 2026-06-27 | 新增 Port/Credential/Domain/Dependency 四个 Controller（各 5 个标准接口，共 20 个），对应 4 张新表 ops_port/ops_credential/ops_domain/ops_dependency；附录 A 补充 4 个数据模型；kb-ops 接口数由 27 更新为 47，总数由 96 更新为 116 | 架构组 |
| v2.3 | 2026-06-28 | 补充遗漏接口并修正标签管理：kb-auth 新增 GET /auth/me（+1）；kb-file 新增 GET /file/{id}/content（+1）；kb-knowledge 新增 PUT /tag/{id} 更新标签、3 个资源标签推荐接口（GET/POST/DELETE /tag/resource）、GET /share/my 我的分享列表、GET /folder/{id} 文件夹详情、GET /space/{id} 空间详情、DELETE /trash/empty 清空回收站、GET /folder/tree 查询参数方式；保留 POST /tag/bind 和 DELETE /tag/unbind 标注兼容旧接口；版本列表接口补充查询参数方式说明；LoginResponse 模型补充 user 字段；各服务接口数更新为 kb-auth:12、kb-file:13、kb-knowledge:51、kb-ops:47，总数更新为 123 | 架构组 |
| v2.4 | 2026-07-03 | 剥离 kb-ops 模块：删除第 5 章 kb-ops 全部 47 个接口（Host/Service/Port/Credential/Domain/Dependency/Conflict/Import 等 Controller）；操作日志接口迁移至 kb-auth，新增 2.13 操作日志列表（`/log/list`，原 `/ops/log/list`）；附录 A 删除 Host/Service/ImportResult/OpsPort/OpsCredential/OpsDomain/OpsDependency 等 kb-ops 数据模型，保留 OperationLog（迁移至 kb-auth）；路由表 `/log/**` 划归 kb-auth；kb-auth 接口数由 12 更新为 13，删除 kb-ops 47 个，总数由 123 更新为 77 | 架构组 |

---

> **接口总数统计**
> - kb-auth：13 个（含操作日志）
> - kb-file：13 个
> - kb-knowledge：51 个
> - **合计：77 个**
