# Private-Knowledge-Base 私有化全端个人知识库

| 版本 | 日期 | 说明 |
|------|------|------|
| v7.1 | 2026-06-29 | 全量测试校准版：补充 kb-intelligence 模块描述，修正接口总数 123→136，新增 TD-10/TD-11 技术债务，链接 v1.5.0 全量测试报告 |
| v7 | 2026-06-28 | 架构校准版：校准部署架构为双层Nginx，补充接口实现状态，修正端口域名 |
| v6 | 2026-06-27 | 微服务重构版：4个微服务拆分完成，P0/P1优化方案落地 |
| v5 | 2026-06-26 | 单体转微服务过渡版 |
| v2 ~ v4 | 2026-06-25 | 早期设计迭代版 |

## 修订记录

- **v7.1（2026-06-29）**：全量测试校准版 — 补充 kb-intelligence 知识引擎模块完整描述（包结构+核心接口），修正服务拆分表（4→6 服务），修正接口总数（123→136），新增 TD-10（PITest 配置缺陷已修复）/TD-11（kb-auth 单元测试覆盖不足待改进）技术债务
- **v7（2026-06-28）**：校准部署架构为双层Nginx，补充接口实现状态，修正端口域名
- **v6（2026-06-27）**：微服务架构定型，kb-auth/kb-file/kb-knowledge/kb-ops 4个微服务拆分完成，P0统一返回格式+全局异常+链路追踪，P1数据一致性+统一日志
- **v5（2026-06-26）**：单体架构转微服务过渡
- **v2 ~ v4（2026-06-25）**：早期设计迭代

---

## 1. 架构设计

### 1.1 部署架构（双层Nginx）

**正确链路：用户 → 腾讯云2号Nginx(:443) → Tailscale(100.93.36.113) → mykng本地Nginx(:80) → kb-gateway(Docker内部,容器8080映射宿主8090) → 各微服务**

```
┌─────────────────────────────────────────────────────────────────┐
│                    用户（浏览器）                                │
└────────────────────────────┬────────────────────────────────────┘
                             │ HTTPS
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│            腾讯云2号 Nginx（公网网关，:443 SSL终结）               │
│              域名：kb.marschat.online                            │
│              纯反向代理，不存静态文件                              │
│              反代 → Tailscale: 100.93.36.113:80                  │
└────────────────────────────┬────────────────────────────────────┘
                             │ Tailscale 内网
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              mykng 本地 Nginx（宿主:80）                          │
│              处理静态资源 + API 反向代理                           │
│              /kb/s/assets/* → alias /data/kb-web/assets/         │
│              /kb/api/*      → 反代 http://127.0.0.1:8090         │
└────────────────────────────┬────────────────────────────────────┘
                             │ HTTP (宿主网络)
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              kb-gateway（Docker容器:8080 → 宿主:8090）            │
│         Spring Cloud Gateway + JWT Auth Filter                   │
│  路由：/api/auth/* → kb-auth:8081（Docker内部）                   │
│        /api/file/* → kb-file:8082（Docker内部）                   │
│        /api/*      → kb-knowledge:8083（Docker内部）              │
│        /api/ops/*  → kb-ops:8084（Docker内部）                    │
└──────┬──────────┬──────────────┬────────────┬───────────────────┘
       │          │              │            │  Docker内部网络
       ▼          ▼              ▼            ▼
┌─────────┐ ┌─────────┐ ┌───────────┐ ┌─────────┐
│ kb-auth │ │ kb-file │ │kb-know-   │ │ kb-ops  │
│ :8081   │ │ :8082   │ │ledge      │ │ :8084   │
│ (内部)  │ │ (内部)  │ │ :8083     │ │ (内部)  │
│         │ │         │ │ (内部)    │ │         │
│ 用户    │ │ 文件    │ │           │ │ 运维    │
│ JWT     │ │ 解析    │ │ 目录      │ │ 主机    │
│ Token   │ │ MinIO   │ │ 笔记      │ │ 端口    │
│         │ │         │ │ 网页      │ │ 账密    │
│         │ │         │ │ 搜索      │ │ 看板    │
│         │ │         │ │ 分享      │ │ 矛盾    │
│         │ │         │ │           │ │ 域名    │
│         │ │         │ │           │ │ 依赖    │
└─────────┘ └─────────┘ └───────────┘ └─────────┘
     │           │           │           │
     └───────────┼───────────┼───────────┘
                 ▼           ▼
          ┌──────────┐ ┌──────────┐
          │ MySQL    │ │ MongoDB  │
          │ 8.0      │ │ 7.0      │
          └──────────┘ └──────────┘
          ┌──────────┐ ┌──────────┐
          │ Redis    │ │ MinIO    │
          │ 7        │ │          │
          └──────────┘ └──────────┘
          ┌──────────┐
          │ Meili-   │
          │ Search   │
          └──────────┘
```

**关键说明：**
- 公网端口只有443（腾讯云2号），mykng的8090/808x不直接暴露公网
- 静态资源由mykng本地Nginx直接提供（/data/kb-web/assets/），不是由网关处理
- 各微服务仅Docker内部通信，不暴露宿主机端口

### 1.2 服务拆分（轻量微服务，适配单机部署）

| 服务 | 职责 | Docker内部端口 | 宿主机映射 | 公网暴露 |
|------|------|---------------|-----------|---------|
| 腾讯云2号Nginx | SSL终结 + 反向代理 | - | 443 | ✅ 是 |
| mykng本地Nginx | 静态资源 + API反代 | - | 80 | ❌ 仅Tailscale内网 |
| kb-gateway | 网关入口 + JWT鉴权 | 8080 | 8090 | ❌ 仅本地Nginx反代 |
| kb-auth | 用户认证/JWT/Token | 8081 | 不映射 | ❌ Docker内部 |
| kb-file | 文件上传/解析/MinIO | 8082 | 不映射 | ❌ Docker内部 |
| kb-knowledge | 目录/笔记/搜索/分享 | 8083 | 不映射 | ❌ Docker内部 |
| kb-ops | 运维管控/主机/端口/凭据 | 8084 | 不映射 | ❌ Docker内部 |
| kb-intelligence | 知识引擎/文档解析/向量检索 | 8086 | 不映射 | ❌ Docker内部 |

**公网访问地址：** https://kb.marschat.online/kb/
**前端base路径：** `/kb/`

### 1.2.1 kb-intelligence 知识引擎模块（v2.3 新增）

kb-intelligence 是 v2.3 落地的知识引擎微服务，实现"机器版 API"用于 AI 消费运维知识。详细设计见 [知识引擎技术方案_v2_可落地.md](知识引擎技术方案_v2_可落地.md)。

**核心能力**：
- **L1 索引层**：`kn_doc` 表存储文档元数据（标题/类型/标签/摘要/SHA256）
- **L2 参数层**：`kn_command`/`kn_timeline` + 复用 kb-ops 的 6 张运维表（host/service/port/credential/domain/dependency）+ `kn_doc_entity_ref` 关联表
- **L3 内容层**：MongoDB `kn_content` 集合按章节切分存储纯文本
- **L4 向量层**：MeiliSearch `kn_vectors` 索引支持关键词/语义/混合搜索

**包结构**：
```
com.kb.intelligence
├── controller/
│   ├── KnowledgeImportController.java     # 路径导入 API
│   └── KnowledgeMachineController.java    # L1-L4 机器版 API
├── entity/                                # 10 个实体（KnDoc/KnCommand/KnTimeline/KnHost/...）
├── mapper/                                # 10 个 Mapper
├── mongo/                                 # MongoDB 内容层
│   ├── doc/KnContent.java
│   ├── repository/KnContentRepository.java
│   └── ContentStorage（接口）+ MongoContentStorage/InMemoryContentStorage
├── parser/                                # 11 个解析器
│   ├── DocType.java                       # TABLE/PLAN/TIMELINE/GRAPH/RULE
│   ├── DocTypeDetector.java               # 类型识别
│   ├── MarkdownParser.java                # 基础 Markdown 解析
│   ├── TableParser.java                   # 类型A：表格解析（零 LLM）
│   ├── PlanDocParser.java                 # 类型B：方案文档（LLM 提取）
│   ├── TimelineParser.java                # 类型C：时间线
│   ├── CommandExtractor.java              # 命令抽取
│   ├── ContentCleaner.java                # 内容精简（Layer 3）
│   ├── LlmClient.java                     # LLM 调用（DashScope）
│   └── GeneralParser.java                 # 通用解析器
├── service/
│   ├── KnowledgeEngine（接口）+ Impl       # 主入口
│   ├── KnowledgeQueryService（接口）+ Impl # L1-L4 查询
│   ├── EntityPersister（接口）+ Impl       # 实体去重落库
│   └── FileScanner（接口）+ Impl           # 文件扫描+增量判断
└── config/DatabaseInitializer.java        # 表结构自动迁移
```

**关键接口**（共 12 个，详见 [知识引擎技术方案_v2_可落地.md](知识引擎技术方案_v2_可落地.md) 第 11 章）：
- `POST /kb/api/intelligence/import/path` — 从文件夹路径导入知识
- `GET /kb/api/intelligence/import/status` — 导入服务健康检查
- `GET /kb/api/intelligence/machine/docs` — L1 文档索引检索（分页）
- `GET /kb/api/intelligence/machine/docs/{docId}/meta` — L1 文档元数据
- `GET /kb/api/intelligence/machine/docs/{docId}/entities` — L2 取实体+参数
- `GET /kb/api/intelligence/machine/docs/{docId}/content` — L3 按章节读内容
- `GET /kb/api/intelligence/machine/entities/hosts` — L2 跨文档查主机
- `GET /kb/api/intelligence/machine/entities/services` — L2 跨文档查服务
- `GET /kb/api/intelligence/machine/entities/commands` — L2 跨文档查命令
- `GET /kb/api/intelligence/machine/entities/timelines` — L2 跨文档查时间线
- `POST /kb/api/intelligence/machine/search` — L4 语义/关键词/混合搜索
- `GET /kb/api/intelligence/machine/stats` — 知识库统计信息

### 1.3 数据库拆分（各服务独立 schema）

| 服务 | 数据库 | 表 |
|------|--------|-----|
| kb-auth | kb_auth | user, refresh_token, jwt_blacklist, ops_api_token |
| kb-file | kb_file | file, file_chunk + MongoDB file_content |
| kb-knowledge | kb_knowledge | folder, doc, web_page, tag, resource_tag, share, share_access_log, version, operation_log, bucket + MongoDB doc_content, web_content |
| kb-ops | kb_ops | ops_host, ops_port, ops_credential, ops_domain, ops_dependency, ops_change_log, ops_conflict, ops_snapshot, ops_service, ops_knowledge, operation_log |
| kb-intelligence | kb_intelligence | kn_doc, kn_command, kn_timeline, kn_doc_entity_ref + MongoDB kn_content + MeiliSearch kn_vectors |

### 1.4 接口实现状态（v2.3接口规范）

| 服务 | 接口数量 | 实现状态 |
|------|---------|---------|
| kb-auth | 12 | ✅ 全部实现并通过测试 |
| kb-file | 13 | ✅ 全部实现并通过测试 |
| kb-knowledge | 52 | ✅ 全部实现并通过测试 |
| kb-ops | 47 | ✅ 全部实现并通过测试 |
| kb-intelligence | 12 | ✅ 全部实现并通过测试（知识引擎 L1-L4 接口） |
| **合计** | **136** | ✅ 所有核心接口已实现并通过测试 |

**E2E 验证**：v1.5.0 全量测试报告中，71 个 E2E 用例覆盖 6 个微服务的核心业务流程，通过率 100%（详见 [全量测试报告](../test-output/reports/全量测试报告.md)）。

**PITest 变异测试**：kb-auth 模块已配置 PITest 变异测试（绑定 verify 阶段），实测变异得分 4%，暴露了单元测试覆盖不足的问题，列入 v1.5.1 改进计划。

---

## 2. 运维数据模型扩展（kb-ops）

> kb-ops 模块在原有基础上，新增 4 张运维数据表，支撑 Port/Credential/Domain/Dependency 四个新 Controller。**注：`ops_change_log` 表对应 `DeploymentRecord` 实体（已存在），此处不重复规划。** 新增列一律以 `ALTER TABLE ADD COLUMN` 方式升级，禁止删表或清空数据。

### 2.1 ops_port（端口表）

记录主机上对外开放或内部监听的端口信息，关联主机与服务。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `BIGINT` | 主键，自增 |
| `host_id` | `BIGINT` | 关联主机 ID（ops_host.id） |
| `port` | `INT` | 端口号 |
| `protocol` | `VARCHAR(16)` | 协议（TCP/UDP/HTTP/HTTPS） |
| `service_id` | `BIGINT` | 关联服务 ID（ops_service.id，可空） |
| `purpose` | `VARCHAR(255)` | 用途说明（如"Nginx 入口"、"MySQL 数据库"） |
| `status` | `TINYINT` | 状态（1=监听中、0=未监听、2=异常） |
| `exposed` | `TINYINT` | 是否对外暴露（1=公网可达、0=仅内网） |
| `remark` | `VARCHAR(500)` | 备注 |
| `created_at` | `DATETIME` | 创建时间 |
| `updated_at` | `DATETIME` | 更新时间 |

**索引**：`idx_host (host_id)`、`idx_service (service_id)`、`uk_host_port (host_id, port)`

### 2.2 ops_credential（凭据表）

集中管理各类账号密码/密钥，密码字段使用 AES-256-GCM 加密存储，列表接口默认不返回明文。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `BIGINT` | 主键，自增 |
| `name` | `VARCHAR(100)` | 凭据名称（如"web-server SSH root"） |
| `type` | `VARCHAR(32)` | 类型（SSH/DB/API_KEY/TOKEN/OTHER） |
| `username` | `VARCHAR(100)` | 用户名 |
| `password_encrypted` | `TEXT` | AES-256-GCM 加密后的密码（密文） |
| `secret_key` | `VARCHAR(255)` | 其他密钥（如 API Token、Access Key） |
| `host_id` | `BIGINT` | 关联主机 ID（可空） |
| `service_id` | `BIGINT` | 关联服务 ID（可空） |
| `remark` | `VARCHAR(500)` | 备注 |
| `created_at` | `DATETIME` | 创建时间 |
| `updated_at` | `DATETIME` | 更新时间 |

**索引**：`idx_type (type)`、`idx_host (host_id)`、`idx_service (service_id)`

### 2.3 ops_domain（域名表）

管理自有域名及 SSL 证书到期信息，辅助运维巡检与告警。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `BIGINT` | 主键，自增 |
| `domain` | `VARCHAR(255)` | 域名（如 kb.marschat.online） |
| `type` | `VARCHAR(32)` | 类型（TOP_LEVEL/SUB_DOMAIN/WILDCARD） |
| `purpose` | `VARCHAR(255)` | 用途说明（如"知识库公网入口"） |
| `registrar` | `VARCHAR(100)` | 注册商（如阿里云、腾讯云） |
| `expires_at` | `DATETIME` | 域名到期时间 |
| `ssl_expires_at` | `DATETIME` | SSL 证书到期时间 |
| `status` | `TINYINT` | 状态（1=正常、0=已过期、2=即将过期） |
| `remark` | `VARCHAR(500)` | 备注 |
| `created_at` | `DATETIME` | 创建时间 |
| `updated_at` | `DATETIME` | 更新时间 |

**索引**：`idx_status (status)`、`uk_domain (domain)`

### 2.4 ops_dependency（依赖关系表）

记录服务间依赖关系，用于拓扑展示与影响面分析。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `BIGINT` | 主键，自增 |
| `service_id` | `BIGINT` | 当前服务 ID（ops_service.id） |
| `service_name` | `VARCHAR(100)` | 当前服务名称（冗余，便于展示） |
| `depends_on_service_id` | `BIGINT` | 依赖的服务 ID |
| `depends_on_service_name` | `VARCHAR(100)` | 依赖的服务名称（冗余） |
| `dependency_type` | `VARCHAR(32)` | 依赖类型（RUNTIME/STARTUP/OPTIONAL/DATA） |
| `remark` | `VARCHAR(500)` | 备注 |
| `created_at` | `DATETIME` | 创建时间 |
| `updated_at` | `DATETIME` | 更新时间 |

**索引**：`idx_service (service_id)`、`idx_depends_on (depends_on_service_id)`

---

## 3. 服务间通信与依赖

### 3.1 服务间通信

- **同步**：OpenFeign（服务间 API 调用）
- **异步**：RabbitMQ（文件解析完成通知等，后续）
- **服务发现**：无（单机部署，直接 HTTP URL）
- **前端部署**：静态资源构建后放置于 `/data/kb-web/`，由 mykng 本地 Nginx 直接处理
  - 访问路径 `/kb/s/assets/` 由本地 Nginx alias 到 `/data/kb-web/assets/`
  - 非静态资源请求由本地 Nginx 反代至 kb-gateway（宿主8090）
  - **不是**"静态资源由网关统一处理"

### 3.2 服务依赖方向

**严格分层，禁止下层调用上层，禁止同层循环调用：**

```
┌─────────────────────────────────────────────────────────────┐
│                  mykng本地Nginx (:80)                        │
│         静态资源处理 + /kb/api/* 反代至网关                   │
└──────────────────────────┬──────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                      kb-gateway (:8080内部/8090宿主)          │
│              统一入口 + JWT 鉴权                              │
└──────┬──────────┬──────────────┬────────────┬──────────────┘
       │          │              │            │
       ▼          ▼              ▼            ▼
  ┌─────────┐ ┌─────────┐ ┌───────────┐ ┌─────────┐
  │ kb-auth │ │ kb-file │ │kb-know-   │ │ kb-ops  │
  │ :8081   │ │ :8082   │ │ledge      │ │ :8084   │
  │         │ │         │ │ :8083     │ │         │
  └─────────┘ └────┬────┘ └─────┬─────┘ └────┬────┘
                   │            │            │
                   │  Feign调用  │            │
                   └───────────►│            │
                                │            │
                                │  Feign调用  │
                                └───────────►│
                                              │
                   ┌──────────────────────────┘
                   │  kb-ops 可调用 kb-knowledge（查目录树）
                   │  kb-ops 可调用 kb-auth（验证 Token）
                   ▼
              【下层服务不反向调用上层】
```

**依赖规则：**

| 调用方 → 被调用方 | 允许 | 说明 |
|-------------------|:----:|------|
| kb-gateway → 任意服务 | ✅ | 网关是入口，可调用所有下游 |
| kb-file → kb-knowledge | ✅ | 文件解析完成后通知建索引 |
| kb-ops → kb-knowledge | ✅ | 运维数据关联目录树 |
| kb-ops → kb-auth | ✅ | 验证 API Token |
| kb-knowledge → kb-auth | ✅ | 验证用户身份 |
| kb-knowledge → kb-file | ⚠️ | **写操作禁止**（通过事件解耦）；**读操作允许**（分享详情、回收站、搜索补全文件元数据，需同步调用）|
| kb-auth → 任意服务 | ❌ | **禁止**，auth 是最底层服务 |
| kb-file → kb-ops | ❌ | **禁止**，无业务关联 |
| 同层互相调用 | ❌ | **禁止循环依赖** |

**解耦规则：**
- 当下层需要通知上层时（如 kb-file 解析完成需要 kb-knowledge 建索引），**不直接 Feign 调用**，改为通过 Redis Pub/Sub 或后续 RabbitMQ 事件通知
- 事件格式统一：`{ "event": "file.parsed", "fileId": 123, "content": "...", "timestamp": "..." }`

### 3.3 技术栈

| 组件 | 选型 |
|------|------|
| 基础框架 | Spring Boot 3.2 + JDK 21 |
| 网关 | Spring Cloud Gateway 4.1 |
| 服务间调用 | OpenFeign |
| ORM | MyBatis-Plus 3.5.6 |
| 认证 | JWT (jjwt 0.12.5) |
| 搜索引擎 | MeiliSearch |
| 对象存储 | MinIO |
| 缓存 | Redis |
| 内容存储 | MongoDB |
| 密码加密 | AES-256-GCM |
| 前端 | Vue3 + ElementPlus + Vite |
| 链路追踪 | MDC + RequestId |
| 日志框架 | Logback |
| 部署 | Docker Compose + 双层Nginx |

---

## 4. P0 优化：统一返回格式 + 全局异常处理

### 4.1 统一返回格式（kb-common 模块）

所有服务统一使用 `Result<T>` 包装返回值：

```java
// kb-common: com.kb.common.result.Result
public class Result<T> {
    private int code;        // 200=成功, 400=参数错误, 401=未认证, 403=无权限, 500=服务异常
    private String message;
    private T data;
    private String traceId;  // 链路追踪 ID

    public static <T> Result<T> success(T data) { ... }
    public static <T> Result<T> error(int code, String message) { ... }
}
```

**前端统一处理：**
```js
// axios 响应拦截器
axios.interceptors.response.use(res => {
  if (res.data.code === 200) return res.data.data;
  if (res.data.code === 401) { /* 跳转登录 */ }
  ElMessage.error(res.data.message);
  return Promise.reject(res.data);
});
```

### 4.2 全局异常处理（kb-common 模块）

```java
// kb-common: com.kb.common.exception.GlobalExceptionHandler
@RestControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(BusinessException.class)
    public Result<?> handleBusiness(BusinessException e) {
        log.warn("业务异常: {}", e.getMessage());
        return Result.error(e.getCode(), e.getMessage());
    }

    @ExceptionHandler(NotLoginException.class)
    public Result<?> handleNotLogin(NotLoginException e) {
        return Result.error(401, "未登录或登录已过期");
    }

    @ExceptionHandler(NoPermissionException.class)
    public Result<?> handleNoPermission(NoPermissionException e) {
        return Result.error(403, "无操作权限");
    }

    @ExceptionHandler(MethodArgumentNotValidException.class)
    public Result<?> handleValidation(MethodArgumentNotValidException e) {
        String msg = e.getBindingResult().getFieldErrors().stream()
            .map(f -> f.getField() + ": " + f.getDefaultMessage())
            .collect(Collectors.joining("; "));
        return Result.error(400, "参数校验失败: " + msg);
    }

    @ExceptionHandler(Exception.class)
    public Result<?> handleUnknown(Exception e) {
        log.error("未知异常", e);
        return Result.error(500, "服务内部错误，请稍后重试");
    }
}
```

**自定义异常体系：**
```
RuntimeException
  └── BusinessException (业务异常基类)
        ├── NotLoginException      (401)
        ├── NoPermissionException  (403)
        ├── NotFoundException      (404, 资源不存在)
        ├── ConflictException      (409, 数据冲突)
        └── FileParseException     (422, 文件解析失败)
```

---

## 5. P0 优化：链路追踪（traceId）

### 5.1 实现方案：MDC + 网关注入

**1. 网关生成 traceId 并注入请求头：**

```java
// kb-gateway: TraceIdFilter
@Component
public class TraceIdFilter implements GlobalFilter, Ordered {
    private static final String TRACE_ID = "X-Trace-Id";

    @Override
    public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {
        String traceId = exchange.getRequest().getHeaders().getFirst(TRACE_ID);
        if (StrUtil.isBlank(traceId)) {
            traceId = UUID.randomUUID().toString().replace("-", "");
        }
        String finalTraceId = traceId;
        return chain.filter(exchange.mutate()
            .request(exchange.getRequest().mutate()
                .header(TRACE_ID, finalTraceId)
                .build())
            .build())
            .then(Mono.fromRunnable(() ->
                exchange.getResponse().getHeaders().add(TRACE_ID, finalTraceId)));
    }

    @Override
    public int getOrder() { return Ordered.HIGHEST_PRECEDENCE; }
}
```

**2. 下游服务从请求头提取 traceId 放入 MDC：**

```java
// kb-common: TraceIdInterceptor
public class TraceIdInterceptor implements HandlerInterceptor {
    @Override
    public boolean preHandle(HttpServletRequest req, HttpServletResponse resp, Object handler) {
        String traceId = req.getHeader("X-Trace-Id");
        if (StrUtil.isBlank(traceId)) {
            traceId = UUID.randomUUID().toString().replace("-", "");
        }
        MDC.put("traceId", traceId);
        resp.setHeader("X-Trace-Id", traceId);
        return true;
    }

    @Override
    public void afterCompletion(HttpServletRequest req, HttpServletResponse resp, Object handler, Exception ex) {
        MDC.remove("traceId");
    }
}
```

**3. Feign 调用时自动传递 traceId：**

```java
// kb-common: FeignTraceIdInterceptor
@Bean
public RequestInterceptor feignTraceIdInterceptor() {
    return template -> {
        String traceId = MDC.get("traceId");
        if (StrUtil.isNotBlank(traceId)) {
            template.header("X-Trace-Id", traceId);
        }
    };
}
```

**4. Result 返回值携带 traceId（方便前端报错时上报）：**

```java
// 在 GlobalExceptionHandler 中注入
String traceId = MDC.get("traceId");
return Result.error(code, message).withTraceId(traceId);
```

**排查示例：**
```
# 拿到前端报错的 traceId，在日志中一键定位全链路
grep "traceId=a1b2c3d4" /data/logs/kb-*.log
```

---

## 6. P1 优化：数据最终一致性方案

### 6.1 问题场景

| 场景 | 涉及服务 | 风险 |
|------|---------|------|
| 文件删除后索引未删 | kb-file + kb-knowledge | 搜索命中已删文件 |
| 文件解析成功但索引写入失败 | kb-file + kb-knowledge | 文件存在但搜不到 |
| 运维数据导入但快照未更新 | kb-ops 内部 | 快照数据不准 |
| 分享链接对应文件已删 | kb-knowledge 内部 | 分享打开 404 |

### 6.2 方案：事件通知 + 定时补偿（双重保障）

**第一层：实时事件通知（Redis Pub/Sub）**

```java
// 事件发布（kb-file 文件解析完成后）
@Service
public class FileParseService {
    @Autowired
    private RedisTemplate<String, String> redisTemplate;

    public void onParseComplete(Long fileId, String content) {
        KbEvent event = new KbEvent("file.parsed", fileId,
            Map.of("content", content), Instant.now());
        redisTemplate.convertAndSend("kb:events", JSON.toJSONString(event));
    }

    public void onFileDeleted(Long fileId) {
        KbEvent event = new KbEvent("file.deleted", fileId,
            Map.of(), Instant.now());
        redisTemplate.convertAndSend("kb:events", JSON.toJSONString(event));
    }
}

// 事件订阅（kb-knowledge 监听并更新索引）
@Service
public class IndexEventListener {
    @Autowired
    private MeiliSearchService searchService;

    @EventListener
    public void onMessage(String message) {
        KbEvent event = JSON.parseObject(message, KbEvent.class);
        switch (event.getEvent()) {
            case "file.parsed" -> searchService.addIndex("kb_files",
                event.getFileId(), event.getPayload().get("content"));
            case "file.deleted" -> searchService.removeIndex("kb_files",
                event.getFileId());
        }
    }
}
```

**第二层：定时补偿任务（每天 03:00 执行）**

```java
// kb-knowledge: ConsistencyCheckTask
@Service
public class ConsistencyCheckTask {

    /**
     * 每天 03:00 执行数据一致性校验
     * 1. 对比 kb_file 表和 MeiliSearch kb_files 索引，修复差异
     * 2. 对比 share 表和 file 表，标记失效分享
     * 3. 将修复记录写入 operation_log
     */
    @Scheduled(cron = "0 0 3 * * ?")
    public void consistencyCheck() {
        // 1. 索引一致性：MySQL 有但 MeiliSearch 没有的 → 补建索引
        List<Long> missingIndexIds = fileMapper.selectIdsNotInSearchIndex();
        missingIndexIds.forEach(id -> {
            File f = fileMapper.selectById(id);
            searchService.addIndex("kb_files", f.getId(), f.getParsedContent());
            log.info("[补偿] 补建索引 fileId={}", id);
        });

        // 2. 索引清理：MeiliSearch 有但 MySQL 已删除的 → 删索引
        List<Long> orphanIndexIds = searchService.selectOrphanIds("kb_files");
        orphanIndexIds.forEach(id -> {
            searchService.removeIndex("kb_files", id);
            log.info("[补偿] 清理孤儿索引 fileId={}", id);
        });

        // 3. 分享有效性：文件已删但分享还在的 → 标记失效
        shareMapper.markInvalidShares();

        log.info("[一致性检查] 完成，补建 {}，清理 {}，标记失效分享 {}",
            missingIndexIds.size(), orphanIndexIds.size(), ...);
    }
}
```

**补偿任务执行表：**

| 检查项 | 频率 | 异常处理 | 日志 |
|--------|------|---------|------|
| MySQL ↔ MeiliSearch 索引一致性 | 每天 03:00 | 自动补建/清理 | operation_log |
| 分享链接有效性 | 每天 03:00 | 标记失效 | operation_log |
| kb-ops 快照完整性 | 每天 03:00 | 重新统计 | ops_change_log |
| 文件-目录关联完整性 | 每周一 03:00 | 修复孤儿文件 | operation_log |

---

## 7. P1 优化：统一日志格式

### 7.1 Logback 配置（所有服务共用，放 kb-common）

```xml
<!-- kb-common: logback-spring.xml -->
<configuration>
    <springProperty scope="context" name="APP_NAME" source="spring.application.name"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{traceId:-}] %-5level ${APP_NAME} - %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>/data/logs/${APP_NAME}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>/data/logs/${APP_NAME}.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>5GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{traceId:-}] %-5level ${APP_NAME} - %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>
```

**日志格式示例：**
```
2026-06-20 14:30:22.456 [http-nio-8083-exec-1] [a1b2c3d4] INFO  kb-knowledge - c.k.k.controller.FileController - 文件上传成功 fileId=123
2026-06-20 14:30:23.789 [http-nio-8083-exec-2] [a1b2c3d4] WARN  kb-knowledge - c.k.k.service.IndexService - 索引写入重试 1/3 fileId=123
2026-06-20 14:30:24.012 [http-nio-8084-exec-1] [e5f6g7h8] ERROR kb-ops       - c.k.o.service.ImportService - 导入失败 host=内网Debian
```

**日志规范：**

| 级别 | 使用场景 | 示例 |
|------|---------|------|
| ERROR | 系统异常、不可恢复错误 | 数据库连接失败、文件解析异常 |
| WARN | 可恢复异常、业务警告 | 索引写入重试、Token 即将过期 |
| INFO | 关键业务节点 | 用户登录、文件上传、导入完成 |
| DEBUG | 调试信息（生产关闭） | SQL 执行、Feign 调用参数 |

**日志目录规划：**

| 服务 | 日志文件 | 轮转策略 |
|------|---------|---------|
| kb-gateway | `/data/logs/kb-gateway.log` | 50MB × 30天 |
| kb-auth | `/data/logs/kb-auth.log` | 50MB × 30天 |
| kb-file | `/data/logs/kb-file.log` | 50MB × 30天 |
| kb-knowledge | `/data/logs/kb-knowledge.log` | 50MB × 30天 |
| kb-ops | `/data/logs/kb-ops.log` | 50MB × 30天 |

**统一排查方式：**
```bash
# 按 traceId 跨服务搜索
grep "a1b2c3d4" /data/logs/kb-*.log

# 按时间范围搜索
awk '$1>="2026-06-20" && $1<="2026-06-21"' /data/logs/kb-*.log

# 按级别过滤
grep " ERROR " /data/logs/kb-*.log | tail -20
```

---

## 8. 实施完成情况

> **最新部署方案详见**：[知识库部署方案_20260628.md](知识库部署方案_20260628.md)

### 8.1 kb-ops Controller 实现情况（12个 = 8原有 + 4新增）

| 序号 | Controller | 路由前缀 | 状态 | 接口数 | 说明 |
|------|-----------|---------|------|--------|------|
| 1 | HostController | `/ops/host` | ✅ 已实现 | 5 | 主机管理（对应 ops_host） |
| 2 | ServiceController | `/ops/service` | ✅ 已实现 | 5 | 服务管理（对应 ops_service） |
| 3 | DeploymentController（=ChangeLog） | `/ops/deployment` | ✅ 已实现 | 3 | 部署记录（对应 ops_change_log，list/recent/POST） |
| 4 | KnowledgeController | `/ops/knowledge` | ✅ 已实现 | 5 | 运维知识（对应 ops_knowledge） |
| 5 | ImportController | `/ops/import` | ✅ 已实现 | 2 | 数据导入（JSON/CSV） |
| 6 | ConflictController | `/ops/conflict` | ✅ 已实现 | 3 | 矛盾检测（detect/list/resolve） |
| 7 | DashboardController | `/ops/dashboard` | ✅ 已实现 | 2 | 运维看板（数据/快照刷新） |
| 8 | OperationLogController | `/ops/log` | ✅ 已实现 | 2 | 操作日志（list/详情） |
| 9 | PortController | `/ops/port` | ✅ 已实现 | 5 | 端口管理（对应 ops_port） |
| 10 | CredentialController | `/ops/credential` | ✅ 已实现 | 5 | 凭据管理（AES-256-GCM加密） |
| 11 | DomainController | `/ops/domain` | ✅ 已实现 | 5 | 域名管理（对应 ops_domain） |
| 12 | DependencyController | `/ops/dependency` | ✅ 已实现 | 5 | 依赖关系（对应 ops_dependency） |

---

## 9. 阶段计划与状态

### 9.1 阶段完成情况

| 阶段 | 主题 | 核心内容 | 状态 |
|------|------|---------|------|
| 阶段一 | 基础框架 | 父工程 + kb-common + 4个微服务骨架 + kb-gateway | ✅ 已完成 |
| 阶段二 | 核心功能 | 用户认证/文件上传解析/目录笔记/网页收藏/全文搜索/MinIO/MeiliSearch/分享功能 | ✅ 已完成 |
| 阶段三 | 分享协作 + 运维管控 | 分享功能/协作能力；kb-ops模块实现47个接口（端口/凭据/域名/依赖服务/主机/服务/部署记录/矛盾检测/运维看板） | ✅ 已完成 |
| 阶段四 | 性能优化 | 缓存优化/查询优化/索引优化/连接池调优 | ⏳ 待开始 |

---

## 10. 待建设与优化路线图

> 以下内容为系统后续版本的演进方向，标注为 **待建设**，作为本系统优化改进的路线规划。
> 当前版本（v2.3）已实现核心微服务链路、统一搜索、运维知识管理、文件管理、分享协作、运维管控等能力。
> 后续版本按优先级分阶段实施，每期上线前需重新评估资源与价值。

### 10.1 路线图总览

| 版本 | 主题 | 核心能力 | 状态 |
|------|------|---------|------|
| v2.3 | 当前版本 | 123个接口 + 双层Nginx部署 + 运维管控47接口 | ✅ 已实现 |
| v2.4 | 性能优化（阶段四） | 缓存/查询/索引/连接池调优 | ⏳ 待开始 |
| v3.0 | 知识关联 | 双向链接 + 图谱视图 | ⏳ 待建设 |
| v3.1 | 效率工具 | 模板系统 + 每日笔记 + 命令面板 | ⏳ 待建设 |
| v3.2 | RAG 检索增强（一期） | 向量索引 + 语义搜索 | ⏳ 待建设 |
| v3.3 | RAG 检索增强（二期） | 关键词 + 向量混合搜索 | ⏳ 待建设 |
| v3.4 | AI 问答助手（三期） | 知识库问答 + 引用溯源 | ⏳ 待建设 |

---

### 10.2 v3.0：知识关联能力（待建设）

**目标**：实现类似 Obsidian 的知识网络化能力，让笔记之间形成关联关系。

#### 双向链接

**功能描述**：
- 笔记正文中支持 `[[笔记标题]]` 语法，自动创建双向引用
- 在笔记详情页底部展示"反向链接"列表（引用了当前笔记的所有笔记）
- 支持悬停预览链接目标笔记摘要

**数据模型变更**（需以 ALTER TABLE 升级方式）：
```sql
-- 笔记关联关系表（新增）
CREATE TABLE IF NOT EXISTS doc_link (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    source_doc_id BIGINT NOT NULL COMMENT '来源笔记ID',
    target_doc_id BIGINT NOT NULL COMMENT '目标笔记ID',
    link_text VARCHAR(255) COMMENT '链接显示文本',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_source (source_doc_id),
    INDEX idx_target (target_doc_id),
    UNIQUE KEY uk_link (source_doc_id, target_doc_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='笔记双向链接关系';
```

**核心 API**：
- `GET /kb/api/doc/{id}/backlinks` — 获取反向链接列表
- `GET /kb/api/doc/{id}/forward-links` — 获取正向链接列表

**实现要点**：
- 笔记保存时解析 `[[xxx]]` 语法，更新 `doc_link` 表
- 反向链接查询走索引，O(1) 复杂度
- 链接目标不存在时，前端支持"点击创建"

#### 图谱视图

**功能描述**：
- 全局知识图谱：可视化所有笔记的关联关系网络
- 局部图谱：以当前笔记为中心，展示 N 度关联网络
- 节点大小按链接数排序，颜色按空间/标签区分
- 支持拖拽、缩放、点击跳转

**前端选型**：Vue3 + vis-network 或 cytoscape.js

**核心 API**：
- `GET /kb/api/graph/full?depth=2` — 全局图谱（限制深度）
- `GET /kb/api/graph/node/{docId}?depth=2` — 局部图谱

**性能考量**：
- 图谱节点超过 500 个时需分页/聚类
- 前端使用 Canvas 渲染（非 SVG）避免 DOM 性能问题

---

### 10.3 v3.1：效率工具集（待建设）

**目标**：提升知识录入和检索效率，向 Obsidian/Notion 级别的笔记体验靠拢。

#### 模板系统

**功能描述**：
- 内置模板：会议纪要、周报、读书笔记、需求文档、技术方案
- 用户自定义模板：支持变量占位符 `{{date}}` `{{title}}` `{{author}}`
- 新建笔记时可选择模板，自动填充内容
- 模板支持分类管理和星标

**数据模型**：
```sql
CREATE TABLE IF NOT EXISTS doc_template (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    name VARCHAR(100) NOT NULL COMMENT '模板名称',
    category VARCHAR(50) COMMENT '分类',
    content LONGTEXT COMMENT '模板内容（含占位符）',
    variables JSON COMMENT '变量定义',
    starred TINYINT DEFAULT 0,
    deleted TINYINT DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_user (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='笔记模板';
```

**核心 API**：
- `GET /kb/api/template/list` — 模板列表
- `POST /kb/api/template` — 创建模板
- `POST /kb/api/template/{id}/apply` — 应用模板创建笔记

#### 每日笔记（Daily Notes）

**功能描述**：
- 每日自动创建一篇以日期命名的笔记（如 `2026-06-26`）
- 支持配置默认模板和默认目录
- 侧边栏快捷入口"今日笔记"
- 历史每日笔记日历视图，快速跳转

**实现要点**：
- 首次访问当日时自动创建（避免定时任务依赖）
- 命名规则可配置：`YYYY-MM-DD` / `YYYY年MM月DD日` / 自定义

#### 命令面板（Command Palette）

**功能描述**：
- 全局快捷键 `Ctrl/Cmd + K` 唤起命令面板
- 支持操作：
  - 快速搜索笔记/文件/网页收藏
  - 快速跳转（空间切换、目录跳转、设置页）
  - 快速操作（新建笔记、上传文件、收藏网页）
  - 模板应用、每日笔记创建
- 模糊匹配 + 最近使用记录

**前端选型**：Vue3 + 自定义组件（参考 raycast/spotlight 交互）

---

### 10.4 v3.2：RAG 检索增强 — 向量索引与语义搜索（待建设）

**目标**：突破关键词字面匹配的限制，实现语义级检索能力。

#### 背景与痛点

当前 MeiliSearch 全文搜索存在以下局限：
- 搜"部署"搜不到标题为"上线流程"的文档
- 搜"性能优化"搜不到内容为"JVM 调优"的笔记
- 无法跨语言匹配（中文搜英文文档）
- 无近义词/概念匹配能力

#### 技术选型

| 组件 | 选型 | 说明 |
|------|------|------|
| 向量数据库 | **MeiliSearch 1.x Hybrid Search** | 复用现有组件，无需新增 |
| 嵌入模型 | **bge-small-zh-v1.5（ONNX）** | 本地推理，~100MB，CPU 可跑 |
| 向量维度 | 512 | bge-small 标准输出 |
| LLM | 暂不接入（v3.4 再做） | 本期仅做检索 |

#### 架构设计

```
写入流程：
  文档/笔记/网页保存
      ↓
  bge-small-zh 生成 embedding（512维向量）
      ↓
  存入 MeiliSearch _vectors 字段

检索流程：
  用户输入查询词
      ↓
  bge-small-zh 生成查询向量
      ↓
  MeiliSearch 向量检索 + 关键词检索（Hybrid）
      ↓
  融合排序返回结果
```

#### 数据模型变更

MeiliSearch 索引新增 `_vectors` 字段：
```json
{
  "id": "15",
  "title": "JVM 调优实践",
  "content": "...",
  "_vectors": {
    "default": [0.0123, -0.0456, ... 512个浮点数]
  }
}
```

#### 实施步骤

1. **集成嵌入模型**：引入 ONNX Runtime + bge-small-zh 模型到 kb-knowledge 服务
2. **改造 SearchIndexService**：写入文档时同步生成 embedding 存入 `_vectors`
3. **改造搜索接口**：新增 `mode=semantic` 参数，调用 MeiliSearch 向量检索
4. **前端适配**：搜索页新增"语义搜索"开关
5. **补偿任务**：ConsistencyCheckTask 增加向量重建逻辑
6. **历史数据迁移**：对已存在的文档批量生成 embedding

#### 预期效果
- 语义搜索：搜"部署"命中"上线流程"
- 跨语言检索：中文搜英文文档
- 近义词匹配：搜"性能优化"命中"调优"

---

### 10.5 v3.3：RAG 检索增强 — 混合搜索（待建设）

**目标**：融合关键词搜索与语义搜索，提供最优检索质量。

#### 功能描述
- Hybrid Search：同时执行关键词检索和向量检索，结果融合排序
- 相关性调优：支持配置关键词权重和向量权重
- 搜索质量评估：对比纯关键词、纯语义、混合三种模式的效果

#### API 设计
```
GET /kb/api/search?q=&mode=hybrid&kwWeight=0.4&vecWeight=0.6
```

#### 实现要点
- MeiliSearch 1.x 原生支持 Hybrid Search，配置 `_vectors` 后自动启用
- 前端默认使用 hybrid 模式，保留切换为 keyword/semantic 的选项

---

### 10.6 v3.4：AI 问答助手（待建设）

**目标**：基于私有知识库的智能问答，实现"问知识库"而非"搜知识库"的交互升级。

#### 功能描述
- 用户自然语言提问 → 向量检索 Top K 相关片段 → LLM 基于片段生成答案
- 答案附带引用来源（文档链接 + 高亮片段）
- 支持多轮对话，上下文记忆
- 跨知识源聚合：回答同时引用文件、笔记、网页、运维记录

#### 技术选型

| 组件 | 选型 | 说明 |
|------|------|------|
| LLM | **Qwen2-1.5B-Q4（本地）** | 已规划，按需启动，~1.5GB 内存 |
| 备选 | 通义千问在线 API | 资源不足时降级方案 |
| 向量检索 | MeiliSearch（v3.2 已建设） | 复用已有索引 |
| Prompt 工程 | RAG 模板 | 拼接 Top K 片段 + 系统提示 |

#### 架构设计

```
用户提问
    ↓
[1] 向量检索：MeiliSearch 召回 Top 5 相关片段
    ↓
[2] Prompt 拼接：系统提示 + 检索片段 + 用户问题
    ↓
[3] LLM 生成：Qwen2 基于上下文生成答案
    ↓
[4] 引用溯源：标注答案来源文档 + 高亮片段
    ↓
返回答案 + 引用列表
```

#### API 设计
```
POST /kb/api/ai/chat
{
  "question": "如何部署应用到生产环境？",
  "history": [...]
}
```

#### 实现要点
- 文档分块：长文档按段落切分（每块 500 字，重叠 100 字）
- 上下文窗口：Top 5 片段，总 token 控制在 2000 以内
- 引用溯源：记录每个片段的 docId + 段落位置
- 安全防护：敏感信息（账密）不进入 LLM 上下文

#### 资源评估
| 组件 | 内存占用 | 启动方式 |
|------|---------|---------|
| Qwen2-1.5B-Q4 | ~1.5GB | 按需启动 |
| bge-small-zh（v3.2 已有） | ~300MB | 常驻 |
| **新增总计** | **~1.5GB** | 需确认 VM 余量 |

---

### 10.7 待建设功能优先级与依赖关系

```
v2.4 性能优化（阶段四，无依赖，可立即开始）
    ↓
v3.0 双向链接 + 图谱视图
    ↓ (无依赖，可独立开发)
v3.1 模板系统 + 每日笔记 + 命令面板
    ↓ (无依赖，可独立开发)
v3.2 RAG 一期：向量索引 + 语义搜索
    ↓ (依赖 v3.2 的向量索引)
v3.3 RAG 二期：混合搜索
    ↓ (依赖 v3.3 的检索能力)
v3.4 AI 问答助手（依赖 v3.2 + v3.3）
```

**实施建议**：
- v3.0 和 v3.1 可并行开发，无技术依赖
- v3.2 → v3.3 → v3.4 为 RAG 链路，需顺序实施
- 每个版本上线前需重新评估 VM 资源（当前 12GB）是否充足
- v3.4 上线前需确认 Qwen2 模型部署就绪

---

### 10.8 已知技术债务（待优化）

> 以下为当前 v2.3 遗留的已知问题，不影响主流程，列入后续优化清单。
>
> **更新（2026-06-26）**：经多轮代码审查与端到端 API 实测，TD-01/TD-02/TD-03/TD-05 已验证修复，TD-04 已收敛。新增 TD-06/TD-07/TD-08 三项日期解析与分页缺陷，均已修复并重新部署验证通过。详见下表状态列。

> **更新（2026-06-27）**：页面功能测试发现 TD-09 — OperationLogController 路径配置错误导致前端调用 500。已修复（Controller 路径统一为 `/ops/log`，新增详情接口）。VM 重建 kb-ops 镜像并部署验证通过。

| 编号 | 问题 | 影响 | 优先级 | 当前状态 |
|------|------|------|--------|----------|
| TD-01 | GetFolderTree 接口在空目录时返回结构异常 | 前端目录树显示异常 | 中 | ✅ 已修复 |
| TD-02 | ShareVerify 接口验证逻辑有 bug | 分享验证偶发失败 | 中 | ✅ 已验证正常 |
| TD-03 | CreateTag 接口存在后端数据/逻辑问题 | 标签创建失败 | 中 | ✅ 已验证正常 |
| TD-04 | dev profile 的 MeiliSearch api-key 与 prod 不一致 | 仅影响本地开发 | 低 | ⚠️ 已收敛 |
| TD-05 | actuator/health 端点未暴露 | 健康检查接口返回 404 | 低 | ✅ 已修复 |
| TD-06 | ShareServiceImpl.create() 使用 LocalDateTime.parse() 解析日期，不支持空格分隔格式 | Share.Create 接口传空格格式 expireAt 返回 500 | 中 | ✅ 已修复 |
| TD-07 | kb-auth 的 MybatisPlusConfig 缺少 PaginationInnerInterceptor 分页插件 | Token.List 接口 total 永远返回 0 | 中 | ✅ 已修复 |
| TD-08 | ApiTokenRequest.expireAt 为 LocalDateTime 类型，Jackson 默认仅接受 ISO 格式 | Token.Create 接口传 `yyyy-MM-dd HH:mm:ss` 格式返回 500 | 中 | ✅ 已修复 |
| TD-09 | OperationLogController 路径配置错误 | 前端调用 500 | 中 | ✅ 已修复 |

> **更新（2026-06-29）**：v1.5.0 全量测试发现 TD-10/TD-11 — PITest 配置缺陷导致变异测试从未真正执行；kb-auth 单元测试覆盖率严重不足。已修复 TD-10 配置问题，TD-11 列入 v1.5.1 改进计划。详见 [全量测试报告](../test-output/reports/全量测试报告.md) 第 4.5 节。

| 编号 | 问题 | 影响 | 优先级 | 当前状态 |
|------|------|------|--------|----------|
| TD-10 | kb-parent/pom.xml 的 PITest 配置仅在 `<pluginManagement>` 中声明，`<build><plugins>` 未声明 pitest-maven，导致子模块即使继承父 POM 也不会触发 PITest 执行 | 变异测试从未真正运行，无法发现单元测试覆盖不足的问题 | 高 | ✅ 已修复（在 kb-auth/pom.xml 显式声明 pitest-maven 插件） |
| TD-11 | kb-auth 模块单元测试覆盖率严重不足：行覆盖率 36%、变异得分 4%、91% 的变异点（690/754）无测试覆盖 | 核心业务类（AuthServiceImpl/UserServiceImpl/ApiTokenServiceImpl/JwtTokenProvider）覆盖率不足，可能存在未发现的代码缺陷 | 高 | ⏳ 列入 v1.5.1 改进计划（目标：变异得分 70%+） |
