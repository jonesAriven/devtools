# STAGING 预发环境：下游服务以容器服务名寻址
kb:
  gateway:
    jwt:
      secret: ${JWT_SECRET:YourSuperSecretKeyForJwtTokenGenerationMustBe256BitsLong!!}
    services:
      auth: ${KB_AUTH_HOST:kb-auth:8081}
      file: ${KB_FILE_HOST:kb-file:8082}
      knowledge: ${KB_KNOWLEDGE_HOST:kb-knowledge:8083}
      intelligence: ${KB_INTELLIGENCE_HOST:kb-intelligence:8086}

logging:
  level:
    com.kb.gateway: INFO
    org.springframework.cloud.gateway: WARN

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
