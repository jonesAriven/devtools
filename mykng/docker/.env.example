# ============================================================
# mykng Docker Compose 环境变量模板
# ============================================================
# 使用: cp .env.example .env 后填入真实值
# 注意: .env 含敏感信息，绝不入库
# ============================================================

# ====== 部署路径 ======
DEPLOY_BASE=/root/kb-deploy

# ====== 统一上下文路径 ======
# 前端 .env VITE_CONTEXT_PATH 必须与此一致
KB_CONTEXT=/kb

# ====== MySQL ======
MYSQL_ROOT_PASSWORD=change-me-mysql-password

# ====== MongoDB ======
MONGO_ROOT_USER=change-me-mongo-user
MONGO_ROOT_PASSWORD=change-me-mongo-password

# ====== MinIO ======
MINIO_ROOT_USER=change-me-minio-user
MINIO_ROOT_PASSWORD=change-me-minio-password

# ====== MeiliSearch ======
MEILI_MASTER_KEY=change-me-meili-master-key
MEILI_KEY=change-me-meili-key

# ====== Nacos ======
# Nacos 服务端用户 nacos 的密码
NACOS_USERNAME=nacos
NACOS_PASSWORD=change-me-nacos-password
NACOS_AUTH_TOKEN=change-me-nacos-token-must-be-32-bytes-or-longer-base64
NACOS_AUTH_IDENTITY_KEY=serverIdentity
NACOS_AUTH_IDENTITY_VALUE=change-me-identity-value

# ====== 应用加密 ======
# AES 加密密钥（用于 auth-center/kb-ops 的密码加密，必须 32 字节）
CRYPTO_AES_KEY=change-me-must-be-32-bytes-exactly!

# JWT 密钥（必须 >= 32 字节）
JWT_SECRET=change-me-jwt-secret-must-be-32-bytes-or-longer

# ====== 菜单上报凭据（Phase 4 P-B，/internal 通道 X-Client-Secret）======
# 值须与 auth-center 侧对应 sys_app_client.client_secret 一致
# （源自 apps-registry.yml 的 menu-report-secret → clients.yml 的 ${ENV:default}）。
# 各应用独立命名，避免共用同一变量名串值：
#  - MARSCHAT_MENU_REPORT_SECRET           → kb-ops（client=marschat-kbops）
#  - KBWEB_MENU_REPORT_SECRET              → kb-gateway 代 kb-web（client=marschat-kbweb）
#  - INFRAMON_MENU_REPORT_SECRET           → infra-monitor（client=marschat-inframon）
MARSCHAT_MENU_REPORT_SECRET=change-me-kbops-menu-report-secret
KBWEB_MENU_REPORT_SECRET=change-me-kbweb-menu-report-secret
INFRAMON_MENU_REPORT_SECRET=change-me-inframon-menu-report-secret
